# 在 Worktree 中启动 VNPy

## 方法1: 使用 worktree 专用启动脚本（推荐）

### 步骤

1. **进入 worktree 目录**
   ```bash
   cd D:\Dev\vnpy-007-multi-timeframe-integration
   ```

2. **运行 worktree 启动脚本**
   ```bash
   update_and_run_worktree.bat
   ```

3. **等待程序启动**
   - 脚本会自动安装依赖
   - 从 worktree 安装修改后的模块
   - 启动 VeighNa Trader

### 与主仓库启动脚本的区别

| 项目 | 主仓库 (dev) | Worktree (007) |
|-----|-------------|---------------|
| 脚本文件 | `update_and_run.bat` | `update_and_run_worktree.bat` |
| 安装路径 | `D:\Dev\vnpy` | `D:\Dev\vnpy-007-multi-timeframe-integration` |
| 功能 | 标准功能 | 包含多周期集成功能 |

## 方法2: 手动启动

### 步骤1: 安装依赖

```bash
cd D:\Dev\vnpy-007-multi-timeframe-integration
pip install -e . --no-deps --no-build-isolation
```

### 步骤2: 安装子模块

```bash
# 安装 vnpy_ctastrategy
cd D:\Dev\vnpy-007-multi-timeframe-integration\vnpy_ctastrategy
pip install -e . --no-deps --no-build-isolation

# 安装 vnpy_ctabacktester
cd D:\Dev\vnpy-007-multi-timeframe-integration\vnpy_ctabacktester
pip install -e . --no-deps --no-build-isolation

# 安装 mflang
cd D:\Dev\vnpy-007-multi-timeframe-integration\mflang
pip install -e . --no-build-isolation

# 安装 indicators
cd D:\Dev\vnpy-007-multi-timeframe-integration\indicators
pip install -e . --no-build-isolation
```

### 步骤3: 启动程序

```bash
cd D:\Dev\vnpy-007-multi-timeframe-integration\examples\veighna_trader
python run.py
```

## 方法3: 从 PyCharm/VSCode 启动

### PyCharm 配置

1. **打开项目**: 打开 `D:\Dev\vnpy-007-multi-timeframe-integration`

2. **配置 Python 解释器**:
   - File → Settings → Project → Python Interpreter
   - 确保使用正确的 Python 环境

3. **配置运行配置**:
   - Run → Edit Configurations
   - Script path: `D:\Dev\vnpy-007-multi-timeframe-integration\examples\veighna_trader\run.py`
   - Working directory: `D:\Dev\vnpy-007-multi-timeframe-integration\examples\veighna_trader`

4. **运行**: 点击绿色三角按钮

### VSCode 配置

1. **打开项目**: 打开 `D:\Dev\vnpy-007-multi-timeframe-integration`

2. **创建 launch.json**:
   ```json
   {
       "version": "0.2.0",
       "configurations": [
           {
               "name": "VNPy Trader (Worktree)",
               "type": "python",
               "request": "launch",
               "program": "${workspaceFolder}/examples/veighna_trader/run.py",
               "cwd": "${workspaceFolder}/examples/veighna_trader",
               "console": "integratedTerminal"
           }
       ]
   }
   ```

3. **运行**: F5 或点击 Run

## 验证多周期功能

程序启动后，验证多周期功能是否正常：

### 1. 打开 K线图表

在 VeighNa Trader 主界面点击"K线图表"按钮

### 2. 检查显示模式下拉框

在 ChartWindow 顶部工具栏，应该能看到：
```
显示模式: [单周期 ▼]
          ├─ 单周期
          └─ 多周期叠加  ← 应该有这个选项
```

### 3. 切换到多周期模式

1. 点击"显示模式"下拉框
2. 选择"多周期叠加"
3. 应该能看到多周期K线叠加显示
4. "多周期设置"按钮应该出现

### 4. 测试多周期设置

1. 点击"多周期设置"按钮
2. 应该弹出设置对话框
3. 可以调整各周期的显示/隐藏和透明度

## 常见问题

### Q1: 启动时提示找不到模块？

**原因**: 可能是 Python 环境路径问题

**解决**:
```bash
# 重新安装主模块
cd D:\Dev\vnpy-007-multi-timeframe-integration
pip install -e . --no-deps --no-build-isolation --force-reinstall
```

### Q2: 界面没有"多周期叠加"选项？

**可能原因**:
1. 使用了主仓库的代码，而不是 worktree 的代码
2. 模块没有正确安装

**验证**:
```bash
python -c "import vnpy; print(vnpy.__file__)"
# 应该显示: D:\Dev\vnpy-007-multi-timeframe-integration\vnpy\__init__.py

python -c "from vnpy.chart import MultiTimeframeWidget; print('OK')"
# 应该显示: OK
```

**解决**: 使用 `update_and_run_worktree.bat` 重新安装

### Q3: 切换到多周期模式后图表空白？

**可能原因**: 没有加载历史数据

**解决**:
1. 点击"查询历史"按钮
2. 或者在"数据源"选择"数据库"，然后点击"加载"

### Q4: 两个环境冲突？

如果同时维护主仓库和 worktree，可能会遇到模块冲突。

**建议方案**:

**方案A: 使用虚拟环境（推荐）**
```bash
# 为 worktree 创建独立虚拟环境
python -m venv D:\Dev\venv-worktree
D:\Dev\venv-worktree\Scripts\activate

# 在虚拟环境中安装
cd D:\Dev\vnpy-007-multi-timeframe-integration
update_and_run_worktree.bat
```

**方案B: 每次切换时重新安装**
```bash
# 切换到主仓库
cd D:\Dev\vnpy
update_and_run.bat

# 切换到 worktree
cd D:\Dev\vnpy-007-multi-timeframe-integration
update_and_run_worktree.bat
```

## 开发流程建议

### 日常开发

1. **在 worktree 中开发**
   ```bash
   cd D:\Dev\vnpy-007-multi-timeframe-integration
   # 修改代码...
   ```

2. **运行测试**
   ```bash
   python -m pytest tests/chart/test_chart_window_mode_switch.py -v
   ```

3. **启动程序验证**
   ```bash
   update_and_run_worktree.bat
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat: 添加xxx功能"
   ```

### 代码审查前

1. **运行所有测试**
   ```bash
   python -m pytest tests/chart/ -v
   ```

2. **代码质量检查**
   ```bash
   python -m ruff check vnpy/chart/
   ```

3. **验证功能完整性**
   - 测试单周期模式
   - 测试多周期模式
   - 测试模式切换
   - 测试画线交易

## 总结

- ✅ **推荐**: 使用 `update_and_run_worktree.bat` 启动
- ✅ **优点**: 自动处理依赖，确保使用 worktree 的代码
- ✅ **注意**: 不要使用主仓库的 `update_and_run.bat`

如有问题，请查看日志输出或联系开发团队。

