# 测试指南：多周期动画边框功能

## 🎯 测试目标

验证以下两个功能是否正常工作：
1. **实时4小时K线矩形边框**（阳线红色顺时针，阴线青色逆时针）
2. **4小时开盘价跑马灯线**（黄色慢速向右）

## 🚀 启动测试

### 方式1：通过主程序启动

```bash
# 启动vnpy主程序
python quick_start.bat

# 或
python 你的启动脚本.py
```

然后：
1. 登录交易接口
2. 打开多周期图表窗口
3. 观察效果

### 方式2：独立测试脚本

创建测试脚本 `test_multi_animation.py`：

```python
"""多周期动画边框测试脚本"""
import sys
from datetime import datetime, timedelta
from vnpy.trader.constant import Exchange
from vnpy.chart.multi_timeframe_widget import MultiTimeframeWidget
from vnpy.trader.ui import create_qapp

def main():
    app = create_qapp()
    
    # 设置时间范围
    end_time = datetime.now()
    start_time = end_time - timedelta(days=7)
    
    # 创建多周期窗口
    widget = MultiTimeframeWidget(
        vt_symbol="MHImain.HKFE",
        exchange=Exchange.HKFE,
        start=start_time,
        end=end_time,
        main_engine=None  # 如果有main_engine，传入
    )
    
    # 加载数据
    widget.load_data()
    
    # 启用实时更新
    widget.enable_realtime()
    
    # 显示窗口
    widget.show()
    widget.resize(1400, 800)
    
    sys.exit(app.exec())

if __name__ == "__main__":
    main()
```

## 📋 测试清单

### 测试1：动画管理器初始化 ✅

**验证项：**
- [ ] 窗口启动后，检查日志是否有 `[多周期] 动画管理器已初始化`
- [ ] 没有初始化错误

**预期结果：**
```
[多周期] 动画管理器已初始化
```

**失败处理：**
- 检查 `_chart` 和 `_first_plot` 是否存在
- 检查导入语句是否正确

---

### 测试2：4小时边框显示（阳线） ✅

**前提条件：**
- 当前4小时K线为阳线（当前价 > 4H开盘价）

**验证项：**
- [ ] 图表上显示红色矩形边框
- [ ] 边框从4小时起始位置到当前位置
- [ ] 边框高线 = 当前收盘价
- [ ] 边框低线 = 4小时开盘价
- [ ] 虚线有顺时针跑马灯动画

**预期日志：**
```
[多周期] 4H边框更新: 阳线 索引[120~180] 价格[26000~26100] 宽度=61
```

**预期效果：**
```
   ┌──────────────────────┐  ← 高线：当前价26100
   │  红色顺时针跑马灯    │  ← 虚线反向流动
   │  线宽3.5px          │
   └──────────────────────┘  ← 低线：开盘价26000
```

---

### 测试3：4小时边框显示（阴线） ✅

**前提条件：**
- 当前4小时K线为阴线（当前价 < 4H开盘价）

**验证项：**
- [ ] 图表上显示青色矩形边框
- [ ] 边框从4小时起始位置到当前位置
- [ ] 边框高线 = 4小时开盘价
- [ ] 边框低线 = 当前收盘价
- [ ] 虚线有逆时针跑马灯动画

**预期日志：**
```
[多周期] 4H边框更新: 阴线 索引[120~180] 价格[25900~26000] 宽度=61
```

**预期效果：**
```
   ┌──────────────────────┐  ← 高线：开盘价26000
   │  青色逆时针跑马灯    │  ← 虚线正向流动
   │  线宽3.5px          │
   └──────────────────────┘  ← 低线：当前价25900
```

---

### 测试4：开盘价跑马灯线 ✅

**验证项：**
- [ ] 图表上显示黄色横线（无限延伸）
- [ ] 横线位置 = 4小时开盘价
- [ ] 虚线有慢速向右跑马灯动画
- [ ] 线宽约1.5像素

**预期日志：**
```
[多周期] 4H开盘价线更新: 26000 (黄色慢速跑马灯)
```

**预期效果：**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━→  黄色慢速跑马灯
                                     ↑ 虚线向右流动
```

---

### 测试5：实时更新（边框扩展） ✅

**测试步骤：**
1. 观察当前边框宽度
2. 等待新的1分钟K线到来
3. 观察边框是否自动扩展

**验证项：**
- [ ] 边框宽度增加1个时间单位
- [ ] 边框右边界移动到新的当前位置
- [ ] 高低线根据新价格调整
- [ ] 动画持续运行

**预期日志：**
```
[多周期Bar] 1分钟K线回调 - 时间: 2025-12-04 15:30, ...
[多周期] 4H边框更新: 阳线 索引[120~181] 价格[26000~26110] 宽度=62
```

---

### 测试6：周期切换 ✅

**测试步骤：**
1. 观察当前4小时边框
2. 等待跨越到下一个4小时周期（如13:00→13:15）
3. 观察边框变化

**验证项：**
- [ ] 旧边框被删除
- [ ] 新边框被创建
- [ ] 新边框从新周期的起始位置开始
- [ ] 开盘价线更新为新周期开盘价

**预期日志：**
```
[多周期] 4小时周期切换，已删除旧边框
[多周期] 4H边框更新: 阳线 索引[240~241] 价格[26050~26060] 宽度=2
```

---

### 测试7：阳线转阴线 ✅

**测试步骤：**
1. 观察红色阳线边框
2. 等待价格下跌至开盘价以下
3. 观察边框颜色和方向变化

**验证项：**
- [ ] 边框颜色从红色变为青色
- [ ] 动画方向从顺时针变为逆时针
- [ ] 高低线位置互换

**预期效果：**
```
阳线 → 阴线
红色顺时针 → 青色逆时针
高线=当前价 → 高线=开盘价
低线=开盘价 → 低线=当前价
```

---

### 测试8：性能测试 ✅

**验证项：**
- [ ] CPU占用 < 5%
- [ ] 内存占用 < 100MB
- [ ] 动画流畅，无卡顿
- [ ] 界面响应正常

**监控方法：**
```bash
# Windows任务管理器
# 观察Python进程的CPU和内存占用
```

---

## 🐛 常见问题排查

### 问题1：看不到边框

**排查步骤：**
1. 检查日志是否有 `[多周期] 动画管理器已初始化`
2. 检查日志是否有 `[多周期] 4H边框更新: ...`
3. 检查 `_animation_manager` 是否为 None
4. 检查数据是否足够（至少1个4小时周期）

**解决方法：**
```python
# 添加调试日志
if widget._animation_manager is None:
    print("❌ 动画管理器未初始化")
else:
    print(f"✅ 动画管理器: {widget._animation_manager.get_line_count()} 条线")
```

### 问题2：边框位置不对

**排查步骤：**
1. 检查 `_get_current_4h_data()` 的返回值
2. 检查索引计算是否正确
3. 检查时区设置

**解决方法：**
```python
# 打印4小时数据
data = widget._get_current_4h_data()
if data:
    print(f"起始索引: {data['start_index']}")
    print(f"当前索引: {data['current_index']}")
    print(f"宽度: {data['current_index'] - data['start_index'] + 1}")
```

### 问题3：没有动画效果

**排查步骤：**
1. 检查定时器是否运行：`widget._animation_manager.is_running()`
2. 检查动画方向设置
3. 检查虚线样式

**解决方法：**
```python
# 检查动画状态
if widget._animation_manager:
    print(f"动画运行: {widget._animation_manager.is_running()}")
    print(f"线条数: {widget._animation_manager.get_line_count()}")
```

### 问题4：周期切换时崩溃

**排查步骤：**
1. 检查异常日志
2. 检查 `_last_4h_period_start` 是否正确更新
3. 检查删除边框的逻辑

**解决方法：**
- 所有删除操作都有异常保护
- 检查日志中的错误信息

## 📊 性能基准

### 预期性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| CPU占用 | < 5% | 包含动画 |
| 内存占用 | < 100MB | 单个边框 |
| 更新延迟 | < 50ms | 每次1分钟K线更新 |
| 动画帧率 | 20 FPS | 定时器50ms |

### 性能测试方法

```python
import time

# 测试更新性能
start = time.time()
widget._update_4h_candle_border()
end = time.time()
print(f"边框更新耗时: {(end - start) * 1000:.2f}ms")

# 测试开盘价线更新
start = time.time()
widget._update_4h_open_price_line_animated()
end = time.time()
print(f"开盘价线更新耗时: {(end - start) * 1000:.2f}ms")
```

## ✅ 验收标准

所有以下项目必须通过：

### 功能性
- [x] 阳线边框正确显示（红色顺时针）
- [x] 阴线边框正确显示（青色逆时针）
- [x] 开盘价线正确显示（黄色慢速）
- [ ] 实时更新正常（边框自动扩展）
- [ ] 周期切换正常（自动删除旧边框）

### 性能
- [ ] CPU占用 < 5%
- [ ] 无内存泄漏
- [ ] 动画流畅无卡顿

### 稳定性
- [ ] 无崩溃
- [ ] 异常被正确捕获
- [ ] 日志正常输出

### 代码质量
- [x] 无Linter错误
- [x] 代码注释完整
- [x] 异常处理完善

## 📝 测试报告模板

```
测试日期：2025-12-04
测试人员：[填写]
测试环境：Windows 10 / Python 3.x

【功能测试】
□ 动画管理器初始化：通过 / 失败 / 未测试
□ 阳线边框显示：通过 / 失败 / 未测试
□ 阴线边框显示：通过 / 失败 / 未测试
□ 开盘价跑马灯线：通过 / 失败 / 未测试
□ 实时更新：通过 / 失败 / 未测试
□ 周期切换：通过 / 失败 / 未测试

【性能测试】
CPU占用：____%
内存占用：____MB
更新延迟：____ms
动画帧率：____FPS

【问题记录】
1. [描述问题]
2. [描述问题]

【总体评价】
通过 / 需要改进 / 不通过

【备注】
[其他说明]
```

## 🔧 快速诊断命令

```python
# Python控制台中执行

# 1. 检查动画管理器
widget._animation_manager  # 应该不为None

# 2. 检查动画状态
widget._animation_manager.is_running()  # 应该为True
widget._animation_manager.get_line_count()  # 应该 >= 2

# 3. 检查4小时数据
data = widget._get_current_4h_data()
print(data)  # 应该返回完整的字典

# 4. 检查边框ID
widget._current_4h_rect_id  # 应该不为None
widget._current_4h_open_line_id  # 应该不为None

# 5. 手动触发更新
widget._update_4h_candle_border()
widget._update_4h_open_price_line_animated()
```

## 📸 截图要求

请截图保存以下场景：

1. **阳线边框**（红色顺时针）
2. **阴线边框**（青色逆时针）
3. **开盘价跑马灯线**（黄色慢速）
4. **实时更新过程**（边框扩展）
5. **周期切换**（旧边框删除，新边框创建）

## 🎬 演示视频

建议录制以下过程：

1. 启动程序
2. 打开多周期窗口
3. 观察边框和开盘价线的动画效果
4. 等待实时更新
5. 观察边框自动扩展

## 📞 支持

测试过程中遇到问题？

1. **查看日志**：所有关键操作都有日志输出
2. **查看文档**：参考实施指南和实现方案
3. **运行演示**：先运行 `demo_drawing_animation.py` 验证基础功能
4. **检查代码**：对比示例代码确认实现正确

---

**祝测试顺利！** 🎉

有问题随时反馈！

