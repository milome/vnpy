# 当前流程分析：多周期模式下点击"加载"按钮

## 完整流程

### 用户操作

```
1. 选择合约: MHImain.HKFE
2. 选择起始时间: 2025-10-02
3. 切换到"多周期叠加"模式
4. 点击"加载"按钮
```

### 系统执行流程

#### 步骤1：加载数据到单周期图表

```python
# ChartWindow.refresh_chart()
self.load_history_data(self.current_vt_symbol)
    ↓
# 在后台线程加载
database.load_bar_data(
    symbol="MHImain",
    exchange=Exchange.HKFE,
    interval=Interval.MINUTE,  # 单周期模式的周期（如1分钟）
    start=用户选择时间,
    end=当前时间
)
    ↓
# 检测并补齐数据缺口
_detect_and_fill_gap(...)
    ↓
# 发送信号
signal_history.emit(data)
```

**耗时**：
- 数据库查询：200-500ms（41280条）
- 缺口检测：100-200ms
- 缺口补齐（如果有）：5-10秒（从FUTU API下载）
- **总计**：约 0.5-10秒（取决于是否有缺口）

#### 步骤2：处理历史数据

```python
# ChartWindow.process_history_data(history)
self.history_data = history  # 缓存数据
self._correct_all_bars_open_price(...)  # 修正开盘价
self.chart.update_history(history)  # 更新单周期图表
    ↓
# 检测到多周期模式
if self.display_mode == "multi":
    self.sync_data_to_multi_timeframe()
```

**耗时**：
- 开盘价修正：50-100ms
- 图表更新：100-200ms
- **总计**：约 150-300ms

#### 步骤3：同步数据到多周期

```python
# ChartWindow.sync_data_to_multi_timeframe()
symbol, exchange = extract_vt_symbol(self.current_vt_symbol)
start_datetime = self.history_data[0].datetime
end_datetime = self.history_data[-1].datetime
    ↓
# 创建进度对话框
progress = MultiTimeframeLoadProgressDialog(self)
progress.show()
    ↓
# 延迟50ms后执行
QTimer.singleShot(50, _load_multi_data)
```

**耗时**：
- 进度对话框创建：10-20ms
- **总计**：约 20ms

#### 步骤4：加载多周期数据（最耗时！）

```python
# MultiTimeframeWidget.switch_symbol()
    ↓
# 4.1 分析数据库状态
optimized_start, need_download = _get_optimized_start_time(database)
    ↓
# 4.2 加载/下载1分钟数据
if need_download:
    # 从 FUTU API 下载
    bars = _download_minute_bars_from_futu(...)
    # 耗时：5-60秒（取决于数据量）
else:
    # 从数据库加载
    bars = database.load_bar_data(..., Interval.MINUTE, ...)
    # 耗时：200-500ms（41280条）
    ↓
# 4.3 并行加载大周期数据
with ThreadPoolExecutor(max_workers=3):
    5m_bars = database.load_bar_data(..., Interval.MINUTE_5, ...)  # 并行
    1h_bars = database.load_bar_data(..., Interval.HOUR, ...)      # 并行
    4h_bars = database.load_bar_data(..., Interval.HOUR_4, ...)    # 并行
    # 耗时：max(500ms, 500ms, 500ms) ≈ 500ms
    ↓
# 4.4 创建图表项
self._manager_5m.update_history(five_minute_bars)  # 50ms
self._item_5m = CrossIndexCandleItem(...)           # 100ms
candle_plot.addItem(self._item_5m)                  # 50ms
# ... 1H, 4H 同样处理
# 耗时：约 600ms
    ↓
# 4.5 更新图表
self._chart.update_history(one_minute_bars)
# 耗时：约 200ms
```

**总耗时**：
- 分析数据库：100ms
- 加载1分钟数据：
  - **从数据库**：200-500ms（快）
  - **从FUTU下载**：5-60秒（慢！⚠️）
- 并行加载大周期：500ms
- 创建图表项：600ms
- 更新图表：200ms
- **总计**：1.6-62秒

## 最耗时的步骤 ⚠️

### 🔴 步骤1：从 FUTU API 下载1分钟数据（如果需要）

**耗时**：5-60秒（取决于数据量）

**数据量估算**：
- 7天：10,080 条 → 约 5-10秒
- 1个月：43,200 条 → 约 20-40秒
- 2个月：86,400 条 → 约 40-80秒
- 3个月：129,600 条 → 约 60-120秒

**为什么慢**：
- FUTU API 下载速度：约 1000-2000 条/秒
- 网络延迟
- API 分页查询

### 🟡 步骤2：从数据库加载1分钟数据

**耗时**：200-500ms

**数据量**：41,280 条（约2个月）

### 🟢 步骤3：并行加载大周期数据

**耗时**：500ms

**优化**：使用 ThreadPoolExecutor 并行加载

### 🟢 步骤4：创建图表项和更新图表

**耗时**：800ms

## 当前行为（根据日志）

从您的日志：
```
数据量: 41280 条
时间范围: 2025-10-02 ~ 2025-12-04（约2个月）
```

**当前执行的步骤**：

1. ✅ **单周期加载**：从数据库加载 41280 条1分钟数据（约 0.5秒）
2. ✅ **数据补齐**：检测缺口，可能从 FUTU API 补充（0-10秒）
3. ✅ **同步到多周期**：
   - 分析数据库状态（0.1秒）
   - 判断：用户选择 2025-10-02 >= 数据库最早时间
   - 策略：**重新下载最近7天**（5-10秒）← **这里最耗时！**
4. ✅ **并行加载大周期**：5m/1H/4H 并行（0.5秒）
5. ✅ **创建图表**：创建图表项（0.8秒）

**总耗时**：约 **7-12秒**

## 性能瓶颈

### 🔴 主要瓶颈：从 FUTU API 下载数据

**问题**：
- 每次切换到多周期模式都会重新下载最近7天
- 即使数据库已有最新数据

**优化建议**：
1. 检查数据库数据是否是最新的（最后一条数据距离现在是否 < 1小时）
2. 如果数据已经很新，跳过下载，直接使用数据库数据
3. 只在数据明显过期时才重新下载

### 优化方案

```python
def _get_optimized_start_time(database):
    db_earliest = query_earliest()
    db_latest = query_latest()  # 新增：查询最新数据
    now = datetime.now()
    
    if user_start < db_earliest:
        # 情况1：数据不全 → 全量下载
        return user_start, True
    elif (now - db_latest) < timedelta(hours=1):
        # 情况2：数据很新（1小时内） → 直接使用数据库，不下载
        logger.info(f"数据库数据很新（最后更新: {db_latest}），跳过下载")
        return user_start, False  # 不需要下载
    else:
        # 情况3：数据有点旧 → 下载最近7天
        return (now - 7天), True
```

**效果**：
- 如果数据库数据很新（< 1小时）：跳过下载，**节省 5-10秒**！
- 总耗时从 7-12秒 → **2-3秒**

您需要我实现这个优化吗？

