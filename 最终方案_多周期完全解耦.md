# 最终方案：多周期完全解耦

## 核心原则

**多周期和单周期完全解耦**：
- 不依赖单周期的 `history_data`
- 不检查单周期是否有数据
- 多周期独立加载，独立判断

## 数据流

### 场景1：用户在单周期加载数据后切换到多周期

```
单周期模式:
  1. 用户选择起始时间：2025-11-20
  2. 点击"加载"按钮
  3. 单周期显示数据
  ↓
切换到多周期:
  1. _switch_to_multi_timeframe_mode()
  2. enable_realtime()  # ✅ 先启用实时更新
  3. _load_multi_timeframe_data()  # ✅ 自动加载
     - 从UI读取起始时间：2025-11-20（保留用户设置）
     - switch_symbol(2025-11-20, now)
     - _check_data_completeness()（智能判断）
       → get_bar_overview()（查询元数据）
       → db_earliest = 2017-11-22
       → user_start = 2025-11-20 >= db_earliest
       → 情况1：数据已下载过
       → 数据年龄 < 1小时
       → 只下载缺口（db_latest ~ now）
  ↓
结果: ✅ 历史K线 + 实时K线
```

### 场景2：用户直接切换到多周期（没有在单周期加载）

```
启动后:
  1. 默认起始时间：7天前
  ↓
切换到多周期:
  1. _switch_to_multi_timeframe_mode()
  2. enable_realtime()
  3. _load_multi_timeframe_data()
     - 从UI读取起始时间：7天前（默认值）
     - switch_symbol(7天前, now)
     - _check_data_completeness()
       → 情况1：数据已下载过
       → 只下载缺口
  ↓
结果: ✅ 历史K线（7天） + 实时K线
```

### 场景3：用户在多周期修改时间后重新加载

```
多周期模式:
  1. 用户修改起始时间：2025-10-01
  2. 点击"加载"按钮
     → refresh_chart()
     → _load_multi_timeframe_data()
     - 从UI读取起始时间：2025-10-01（新值）
     - switch_symbol(2025-10-01, now)
     - _check_data_completeness()
       → 情况1：数据已下载过
       → 数据年龄 < 1小时
       → 只下载缺口
  ↓
结果: ✅ 更新为2个月数据 + 实时K线
```

## 关键代码

### `_switch_to_multi_timeframe_mode()`

```python
def _switch_to_multi_timeframe_mode(self):
    # 1. 显示多周期Widget
    self.chart.setVisible(False)
    self.multi_timeframe_widget.setVisible(True)
    
    # 2. 启用画线交易功能
    self.multi_timeframe_widget.enable_drawing_order(...)
    
    # 3. ✅ 先启用实时更新
    self.multi_timeframe_widget.enable_realtime()
    
    # 4. ✅ 自动加载数据
    self._load_multi_timeframe_data()
```

### `_load_multi_timeframe_data()`

```python
def _load_multi_timeframe_data(self):
    # 1. 从UI控件读取当前设置的起始时间
    user_start = self.start_datetime.dateTime().toPython()
    end = datetime.now()
    
    # 2. 异步加载数据
    def load_data():
        self.multi_timeframe_widget.switch_symbol(
            vt_symbol=symbol,
            exchange=exchange,
            start=user_start,  # ✅ 保留用户设置
            end=end,
            progress_callback=...
        )
        # ✅ 加载完成后再次启用实时更新（确保BarGenerator正确初始化）
        self.multi_timeframe_widget.enable_realtime()
    
    QTimer.singleShot(50, load_data)
```

### `switch_symbol()` → `_load_data_and_build_items()`

```python
def _load_data_and_build_items(self):
    # 1. 从数据库加载1分钟数据
    one_minute_bars = database.load_bar_data(
        start=self._start,  # ✅ 用户设置的时间
        end=self._end
    )
    
    # 2. 智能下载策略
    download_start, download_end, need_download = self._check_data_completeness(
        database, one_minute_bars, self._start, self._end
    )
    
    # 3. 如需下载
    if need_download:
        downloaded_bars = self._download_from_futu(download_start, download_end)
        # 重新加载完整范围
        one_minute_bars = database.load_bar_data(self._start, self._end)
    
    # 4. 聚合大周期数据
    ...
```

### `_check_data_completeness()`

```python
def _check_data_completeness(...):
    # 1. 使用 get_bar_overview() 查询元数据
    overviews = database.get_bar_overview()
    target_overview = [筛选当前合约]
    
    db_earliest = target_overview.start  # 2017-11-22
    db_latest = target_overview.end      # 2025-12-04 15:02:00
    
    # 2. 统一时区转换
    # 所有时间转换到 Asia/Shanghai
    
    # 3. 判断情况
    if user_start >= db_earliest:
        # 情况1：数据已下载过
        if data_age >= 1小时:
            return user_end - 7天, user_end, True
        else:
            return db_latest, user_end, True  # ✅ 只下载缺口
    else:
        # 情况2：数据不全
        return user_start, user_end, True  # 全量下载
```

## 优势

### ✅ 完全解耦

- 多周期不依赖单周期数据
- 单周期和多周期可以独立操作
- 不会互相干扰

### ✅ 保留用户设置

- 用户在单周期设定的时间会保留
- 切换到多周期时使用这个时间
- 用户体验连贯

### ✅ 智能下载

- 自动判断是否需要下载
- 情况1：只下载缺口（高效）
- 情况2：全量下载（完整）

### ✅ 实时更新

- 切换时立即启用实时更新
- 数据加载后BarGenerator正确初始化
- 实时K线正常显示

## 测试验证

### 测试1：单周期设定时间后切换

```
操作:
  1. 单周期模式，选择 2025-11-20
  2. 加载数据
  3. 切换到多周期

预期:
  - ✅ 自动加载 2025-11-20 ~ now 的数据
  - ✅ 使用智能下载策略（只下载缺口）
  - ✅ 显示历史K线和实时K线
```

### 测试2：直接切换到多周期

```
操作:
  1. 启动程序
  2. 直接切换到多周期

预期:
  - ✅ 使用默认时间（7天前）
  - ✅ 自动加载数据
  - ✅ 显示历史K线和实时K线
```

### 测试3：多周期修改时间后重新加载

```
操作:
  1. 多周期模式
  2. 修改起始时间：2025-10-01
  3. 点击"加载"

预期:
  - ✅ 加载 2025-10-01 ~ now 的数据
  - ✅ 实时K线继续工作
```

完全解耦，逻辑清晰！🎉

