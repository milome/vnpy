# 触发机制保护措施检查报告

**日期**: 2025-12-04  
**版本**: v1.0  
**文件**: `vnpy/chart/widget_trigger.py`

---

## 📋 概述

本文档详细记录了挂单触发、止损触发、止盈触发三个核心方法的保护机制，包括 RLock、防抖保护、未成交订单检查等多重保护措施。

---

## 🔒 三个触发方法的保护机制详情

### 1. **挂单触发** (`trigger_pending_order_breakthrough`)

#### 1.1 基本信息
- **方法位置**: 第19-508行
- **功能**: 当价格突破挂单线时，发送开仓订单

#### 1.2 保护机制

| 保护类型 | 位置 | 说明 |
|---------|------|------|
| **RLock 保护** | 第37行 | `with self._pending_order_trigger_lock` |
| **防重复触发** | 第38-60行 | 检查挂单线是否已关联活跃订单 |
| **检查未成交订单** | 第38-60行 | 如果已关联活跃订单，跳过触发 |

#### 1.3 防重复触发逻辑

```python
# ✅ 防重复触发机制：检查挂单线是否已经关联了订单（防止重复触发）
controller = self.get_drawing_order_controller()
if controller and hasattr(controller, 'get_order_id_for_line'):
    existing_order_id = controller.get_order_id_for_line(line_id)
    if existing_order_id:
        # 检查订单是否仍然活跃（未成交、未撤销）
        if main_engine:
            order = main_engine.get_order(existing_order_id)
            if order and order.is_active():
                # 挂单线已经关联了活跃订单，跳过触发（防止重复下单）
                main_engine.write_log(
                    f"[ChartWidget] [挂单触发跳过] 挂单线 {line_id[-8:]} 已关联活跃订单 {existing_order_id[-8:]}, 跳过重复触发",
                    "ChartWidget"
                )
                return False
```

#### 1.4 特点
- 使用 RLock 确保线程安全
- 通过检查订单关联关系防止重复下单
- 支持主力合约映射

---

### 2. **止损触发** (`trigger_stop_loss_close`)

#### 2.1 基本信息
- **方法位置**: 第510-832行
- **功能**: 当价格突破止损线时，发送平仓订单

#### 2.2 保护机制

| 保护类型 | 位置 | 时间/条件 | 说明 |
|---------|------|----------|------|
| **RLock 保护** | 第538行 | - | `with self._stop_loss_trigger_lock` |
| **防抖保护** | 第540-560行 | **3秒** | 同一止损线3秒内不重复触发 |
| **创建时间检查** | 第569-577行 | **500ms** | 激活后500ms内忽略触发 |
| **拖拽保护期** | 触发前检查 | **500ms** | 拖拽结束后500ms内不触发 |
| **检查未成交订单** | 第669-731行 | - | 检查是否已有未成交的平仓订单 |
| **检查可用持仓** | 第645-653行 | - | 检查持仓是否被冻结（正在平仓中） |

#### 2.3 防抖保护逻辑

```python
# ✅ 防重复触发机制：检查是否已经触发过（防止短时间内重复触发）
from time import time
trigger_key = f"stop_loss_{line_id}"
current_time = time()

# 检查是否已有触发记录（防抖：3秒内不重复触发）
if hasattr(self, '_trigger_records'):
    last_trigger_time = self._trigger_records.get(trigger_key, 0)
    time_since_trigger = current_time - last_trigger_time
    
    # 如果防抖期内，直接跳过
    if time_since_trigger < 3.0:  # 3秒内直接跳过
        if main_engine:
            main_engine.write_log(
                f"[ChartWidget] 触发止损跳过: 止损线 {line_id} 在 {time_since_trigger:.2f} 秒前已触发，防抖保护",
                "ChartWidget"
            )
        return False
```

#### 2.4 创建时间检查逻辑

```python
# ✅ 检查止损线是否已激活：只有已激活的止损线才能触发
creation_time = line.get_creation_time()
if creation_time is None:
    # 止损线未激活（关联挂单线但挂单未成交），静默跳过
    return False

# ✅ 检查创建时间：如果刚激活（<500ms），忽略触发（防止激活后立即触发）
time_since_creation = current_time - creation_time
if time_since_creation < 0.5:  # 500ms内忽略触发
    if main_engine:
        main_engine.write_log(
            f"[ChartWidget] 触发止损跳过: 止损线 {line_id} 刚激活 {time_since_creation:.3f} 秒前，忽略触发（防误触发）",
            "ChartWidget"
        )
    return False
```

#### 2.5 检查未成交订单逻辑

```python
# ✅ 防重复下单：检查是否已有未成交的平仓订单（支持主力合约映射）
all_active_orders = main_engine.get_all_active_orders()
existing_close_orders = []
for order in all_active_orders:
    if order.direction == close_direction and order.offset == Offset.CLOSE and order.is_active():
        # 匹配合约和方向
        existing_close_orders.append(order)

if existing_close_orders:
    # 已有未成交的平仓订单，跳过（并更新防抖时间）
    order_info = ", ".join([
        f"{order.vt_orderid} ({order.status.value})"
        for order in existing_close_orders
    ])
    if main_engine:
        main_engine.write_log(
            f"[ChartWidget] 触发止损跳过: 止损线 {line_id} 已有未成交的平仓订单: {order_info}",
            "ChartWidget"
        )
    # 更新防抖时间，防止在订单未成交期间重复检查
    self._trigger_records[trigger_key] = current_time
    return False
```

#### 2.6 特点
- **五层保护**：RLock → 防抖 → 创建时间 → 未成交订单 → 可用持仓
- 支持主力合约映射
- 使用对手价 + 智能追价（重试2次）

---

### 3. **止盈触发** (`trigger_take_profit_close`)

#### 3.1 基本信息
- **方法位置**: 第834-1170行
- **功能**: 当价格突破止盈线时，发送平仓订单

#### 3.2 保护机制

| 保护类型 | 位置 | 时间/条件 | 说明 |
|---------|------|----------|------|
| **RLock 保护** | 第855行 | - | `with self._take_profit_trigger_lock` |
| **防抖保护** | 第857-877行 | **3秒** | 同一止盈线3秒内不重复触发 |
| **创建时间检查** | 第886-894行 | **500ms** | 激活后500ms内忽略触发 |
| **拖拽保护期** | 触发前检查 | **500ms** | 拖拽结束后500ms内不触发 |
| **检查未成交订单** | 第1011-1073行 | - | 检查是否已有未成交的平仓订单 |
| **检查可用持仓** | 第987-995行 | - | 检查持仓是否被冻结（正在平仓中） |

#### 3.3 保护逻辑

与止损触发类似，止盈触发也实现了相同的五层保护机制：

1. **RLock 保护** - 确保线程安全
2. **防抖保护（3秒）** - 防止短时间内重复触发
3. **创建时间检查（500ms）** - 防止激活后立即触发
4. **未成交订单检查** - 防止重复下单
5. **可用持仓检查** - 防止在持仓冻结时触发

#### 3.4 特点
- 保护机制与止损触发完全一致
- 触发条件相反（多仓止盈：价格高于止盈线；空仓止盈：价格低于止盈线）
- 同样支持主力合约映射

---

## 📊 保护机制对比表

| 保护机制 | 挂单触发 | 止损触发 | 止盈触发 | 备注 |
|---------|---------|---------|---------|------|
| **RLock** | ✅ | ✅ | ✅ | 所有三个方法都使用独立的 RLock |
| **防抖保护** | ✅（检查已关联订单） | ✅（3秒） | ✅（3秒） | 挂单用订单关联检查，止损/止盈用时间防抖 |
| **创建时间检查** | ❌ N/A | ✅（500ms） | ✅（500ms） | 挂单线无需此检查 |
| **拖拽保护期** | ❌ N/A | ✅（500ms） | ✅（500ms） | 仅适用于止损/止盈 |
| **检查未成交订单** | ✅ | ✅ | ✅ | 所有三个方法都检查 |
| **检查可用持仓** | ❌ N/A | ✅ | ✅ | 挂单是开仓，无需检查持仓 |

---

## ⚙️ RLock 初始化

所有三个锁都在 `ChartWidget.__init__` 中初始化（`widget.py` 第133-136行）：

```python
from threading import RLock
self._stop_loss_trigger_lock = RLock()       # 保护止损线触发平仓逻辑
self._take_profit_trigger_lock = RLock()     # 保护止盈线触发平仓逻辑
self._pending_order_trigger_lock = RLock()   # 保护挂单线触发下单逻辑
```

---

## 🎯 设计原则

### 1. **多层防护**
- 每个触发方法都实现了多层防护机制
- 从线程安全 → 防抖 → 业务逻辑检查，层层把关

### 2. **时间窗口**
- **防抖保护（3秒）**: 防止同一价格线短时间内重复触发
- **创建时间检查（500ms）**: 防止挂单成交后立即触发止损/止盈
- **拖拽保护期（500ms）**: 防止拖拽结束后立即触发

### 3. **业务逻辑检查**
- **未成交订单检查**: 防止在已有订单的情况下重复下单
- **可用持仓检查**: 防止在持仓冻结（正在平仓中）时重复平仓
- **订单关联检查**: 防止同一挂单线重复触发

### 4. **主力合约支持**
- 所有检查都支持主力合约映射
- 图表显示主力合约（如 `MHImain.HKFE`），实际交易是具体合约（如 `MHI2512.HKFE`）

---

## ⚠️ 注意事项

### 1. 防抖时间差异
- **止损/止盈的防抖时间是3秒**（`time_since_trigger < 3.0`）
- **拖拽保护期是500ms**（`time_since_creation < 0.5`）
- 这两个是不同的保护机制，各有其用途：
  - **3秒防抖**: 防止在订单未成交期间重复检查和下单
  - **500ms保护**: 防止激活/拖拽后立即触发

### 2. 用户历史要求
- 用户之前要求"**保护期统一设置为500ms**"是指**拖拽保护期**，已完成 ✅
- **防抖时间3秒**的目的是防止在订单未成交期间重复检查，这是合理的设计
- 如果需要修改防抖时间，需要权衡以下因素：
  - 时间过短：可能导致订单未成交时重复下单
  - 时间过长：可能导致价格快速反转时无法及时触发

### 3. 挂单触发的特殊性
- 挂单触发不使用时间防抖，而是使用**订单关联检查**
- 这是因为挂单触发后立即关联订单，可以通过订单状态判断是否已触发
- 止损/止盈触发后需要等待订单下单和成交，所以需要时间防抖

---

## 📝 修改记录

| 日期 | 修改内容 | 提交ID |
|------|---------|--------|
| 2025-12-04 | 添加挂单触发防抖机制（检查已关联订单） | 2af5f882 |
| 2025-12-04 | 统一拖拽保护期为500ms | 之前的提交 |
| 2025-12-04 | 修复 `direction.value` AttributeError | 2af5f882 |

---

## ✅ 结论

所有三个触发方法都已正确实施 **RLock + 防抖保护 + 未成交订单检查** 的多重保护机制，符合设计要求。

### 核心优势
1. **线程安全**: 使用 RLock 确保并发安全
2. **防重复触发**: 多层检查机制，避免重复下单
3. **用户友好**: 拖拽保护期确保用户操作体验
4. **业务完整**: 支持主力合约映射、可用持仓检查等

### 潜在优化方向
- 如果需要，可以将止损/止盈的3秒防抖时间调整为更短（如1秒或500ms）
- 但需要确保不会导致订单未成交时重复下单的问题

---

**文档生成时间**: 2025-12-04  
**最后更新**: 2025-12-04

