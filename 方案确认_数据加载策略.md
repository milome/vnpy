# 方案确认：多周期数据加载策略

## 现状分析

### 已有的聚合函数

发现代码库中已有聚合函数：

1. **vnpy/chart/four_hour_aggregator.py**
   - `aggregate_to_4hours(one_minute_bars: list[BarData]) -> list[BarData]`

2. **examples/candle_chart/five_minute_aggregator.py**
   - `aggregate_to_5minutes(one_minute_bars: List[BarData]) -> List[BarData]`

3. **examples/candle_chart/one_hour_aggregator.py**
   - `aggregate_to_1hour(one_minute_bars: List[BarData]) -> List[BarData]`

4. **vnpy/trader/period_utils.py**
   - `aggregate_to_1hour_from_minutes(...)`

5. **vnpy_datamanager**
   - `aggregate_5minute_bars(...)`
   - `aggregate_hour_bars(...)`
   - `aggregate_4hour_bars(...)`

### 当前实现

`MultiTimeframeWidget` 目前是**直接从数据库加载各周期数据**：

```python
# 当前代码（已注释聚合逻辑）
five_minute_bars = database.load_bar_data(..., Interval.MINUTE_5)
# one_hour_bars = aggregate_to_1hour(one_minute_bars)  # ← 已注释
one_hour_bars = database.load_bar_data(..., Interval.HOUR)
```

## 方案讨论

### 方案对比

| 维度 | 数据库加载 (当前) | 内存聚合 (建议) |
|------|------------------|----------------|
| **速度** | 快（并行：500ms） | 中（串行：170ms + 500ms = 670ms） |
| **准确性** | ⚠️ 可能不准确 | ✅ 准确 |
| **开盘价** | ❌ 已发现问题 | ✅ 保证正确 |
| **对齐性** | ⚠️ 可能不对齐 | ✅ 完美对齐 |
| **存储** | 需要预存 | 只存1分钟 |
| **维护** | 需要同步更新 | 自动一致 |

### 我的建议

**推荐方案：混合方案**

```
1. 优先从数据库加载各周期数据（快速）
2. 如果大周期数据缺失或不完整，从1分钟聚合（准确）
3. 实时K线：始终使用内存聚合（与 BarGenerator 逻辑一致）
```

#### 实现逻辑

```python
# 第一步：加载1分钟数据（必需）
one_minute_bars = load_one_minute_bars_optimized(...)  # 优化起始时间

# 第二步：尝试从数据库加载大周期，失败则聚合
def load_or_aggregate(interval, interval_name):
    # 尝试从数据库加载
    bars = database.load_bar_data(..., interval)
    
    # 如果数据缺失或数量不匹配，从1分钟聚合
    expected_count = estimate_bar_count(one_minute_bars, interval)
    if not bars or len(bars) < expected_count * 0.9:  # 允许10%误差
        logger.info(f"{interval_name} 数据不完整，从1分钟聚合")
        bars = aggregate_function(one_minute_bars)
    
    return bars

# 并行加载/聚合大周期
with ThreadPoolExecutor(max_workers=3) as executor:
    future_5m = executor.submit(load_or_aggregate, Interval.MINUTE_5, '5分钟')
    future_1h = executor.submit(load_or_aggregate, Interval.HOUR, '1小时')
    future_4h = executor.submit(load_or_aggregate, Interval.HOUR_4, '4小时')
```

## 关于起始时间优化

### 问题场景

```
用户选择: 2024-01-01（1年前）
数据库最早: 2025-09-01（3个月前）

如果按用户选择加载：
- 实际只能加载到 2025-09-01（数据库没有更早的）
- 但查询了1年范围，浪费时间
```

### 优化方案

```python
def get_optimal_start_time(user_start: datetime, symbol: str, exchange: Exchange) -> datetime:
    """
    优化起始时间
    
    策略：
    1. 查询数据库最早数据时间
    2. 如果用户选择早于数据库最早时间，只加载最近7天
    3. 否则使用用户选择
    """
    # 查询数据库最早数据
    db_earliest = database.get_bar_data_earliest(symbol, exchange, Interval.MINUTE)
    
    if not db_earliest:
        # 数据库没有数据，只加载最近7天
        return datetime.now() - timedelta(days=7)
    
    if user_start < db_earliest:
        # 用户选择时间早于数据库，只加载最近7天
        actual_start = datetime.now() - timedelta(days=7)
        logger.info(
            f"[优化] 用户选择时间 {user_start} 早于数据库最早数据 {db_earliest}，"
            f"优化为加载最近7天: {actual_start}"
        )
        return actual_start
    
    # 使用用户选择
    return user_start
```

### 配置化

可以添加配置项：

```python
# 配置：当用户选择时间过早时，默认加载天数
DEFAULT_LOAD_DAYS = 7  # 可配置为 7/30/90

# 或者弹出询问对话框
if user_start < db_earliest:
    reply = QtWidgets.QMessageBox.question(
        self,
        "时间范围优化",
        f"数据库最早数据为 {db_earliest}，\n"
        f"您选择的 {user_start} 早于此时间。\n\n"
        f"建议只加载最近7天数据以提高速度。\n\n"
        f"是否使用优化加载？",
        QtWidgets.QMessageBox.Yes | QtWidgets.QMessageBox.No
    )
    if reply == QtWidgets.QMessageBox.Yes:
        actual_start = datetime.now() - timedelta(days=7)
```

## 您的决策

请确认以下方案：

### 问题1：起始时间优化
- [ ] A. 自动优化：早于数据库最早时间，自动改为7天
- [ ] B. 询问用户：弹出对话框让用户选择
- [ ] C. 配置化：在设置中可配置默认天数

### 问题2：大周期数据策略
- [ ] A. 纯聚合：从1分钟内存聚合所有大周期
- [ ] B. 混合方案：优先数据库，缺失时聚合
- [ ] C. 纯数据库：直接加载各周期（当前）

我的推荐：
- **问题1**：选择 **A（自动优化）**，简单快速
- **问题2**：选择 **B（混合方案）**，兼顾性能和准确性

您的意见？

