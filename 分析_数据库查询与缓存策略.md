# 数据库查询分析与缓存策略

## 数据库查询位置

### 查询点1：`update_tick()` - 1分钟K线开盘价修正

**位置**：`multi_timeframe_widget.py` 第1355-1361行

```python
database = get_database()
minute_end = bar_minute_start + timedelta(minutes=1)
db_bars = database.load_bar_data(
    symbol=self._vt_symbol,
    exchange=self._exchange,
    interval=Interval.MINUTE,
    start=bar_minute_start,      # 例如: 19:29:00
    end=minute_end                # 例如: 19:30:00
)
```

### 查询点2：`_get_large_timeframe_open_price()` - 大周期开盘价修正

**位置**：`multi_timeframe_widget.py` 第1669-1675行

```python
# 方法1：查询大周期K线
db_bars = database.load_bar_data(
    symbol=self._vt_symbol,
    exchange=self._exchange,
    interval=interval,            # 5m / 1H / 4H
    start=large_bar_datetime,     # 例如: 19:25:00 (5m周期)
    end=period_end                # 例如: 19:30:00 (5m周期)
)
```

**位置**：`multi_timeframe_widget.py` 第1696-1702行

```python
# 方法2：查询1分钟K线
bars_1m = database.load_bar_data(
    symbol=self._vt_symbol,
    exchange=self._exchange,
    interval=Interval.MINUTE,
    start=large_bar_datetime,     # 例如: 19:25:00
    end=large_bar_datetime + timedelta(minutes=1)  # 例如: 19:26:00
)
```

## 详细分析

### 1. 什么时候调用？获取多大范围的数据？

#### 查询点1：1分钟开盘价修正

**调用时机**：
- 每次收到tick时，如果该分钟的开盘价**尚未缓存**
- 并且历史数据（`_main_manager`）中没有该分钟的K线

**查询范围**：
- **1分钟** 的数据（start → start+1分钟）
- 例如：`19:29:00` → `19:30:00`
- **只返回1根K线**（如果存在）

**目的**：
- 获取该分钟K线的准确开盘价
- 修正 `BarGenerator` 可能不准确的开盘价

#### 查询点2：大周期开盘价修正

**调用时机**：
- 每次 `update_tick()` 调用 `_update_large_timeframe_bar()` 时
- 对每个大周期（5m/1H/4H）都调用一次 `_get_large_timeframe_open_price()`
- **每个tick都会调用 3次**（分别对应 5m, 1H, 4H）

**查询范围（方法1 - 查询大周期K线）**：
- **5分钟周期**：`start` → `start + 5分钟`
- **1小时周期**：`start` → `start + 1小时`
- **4小时周期**：`start` → `period_end`（HKFE规则）
- **每次只返回1根K线**（该周期的历史K线）

**查询范围（方法2 - 查询1分钟K线）**：
- **1分钟** 的数据（大周期的开始时间 → +1分钟）
- 例如：`19:25:00` → `19:26:00`（5分钟周期的第一分钟）
- **只返回1根K线**（该大周期的第一根1分钟K线）

**目的**：
- 获取大周期K线的准确开盘价
- 优先从历史大周期K线获取，其次从第一根1分钟K线获取

### 2. 调用频率有多密集？

#### 场景分析

假设每秒收到 **3个tick**，每分钟收到 **180个tick**。

#### 查询点1：1分钟开盘价修正

**优化前（无缓存）**：
- 每个tick都可能触发查询
- 每分钟最多 **180次** 数据库查询（历史数据未命中的情况）

**优化后（有缓存）**：
- 每分钟只在**第一个tick**时查询
- 每分钟最多 **1次** 数据库查询 ✅
- 后续179个tick使用缓存

#### 查询点2：大周期开盘价修正

**当前状态（无缓存）**：
- 每个tick都调用 `_update_large_timeframe_bar()` **3次**（5m/1H/4H）
- 每次调用都可能触发 `_get_large_timeframe_open_price()`
- 最坏情况（历史数据未命中）：
  - **方法1查询1次（大周期K线）**
  - **方法2查询1次（1分钟K线）**
  - 共 **2次查询/大周期**
  
**密集度计算**：
```
每个tick：
  - 5m周期：2次查询（最坏）
  - 1H周期：2次查询（最坏）
  - 4H周期：2次查询（最坏）
  - 合计：6次查询/tick

每秒3个tick：
  - 6 × 3 = 18次查询/秒

每分钟180个tick：
  - 6 × 180 = 1080次查询/分钟 ❌ 极度密集
```

**实际情况可能更好**：
- 如果方法1查询成功，方法2不会执行
- 但每个大周期每个tick仍然会查询1次
- **每秒约 9次查询**（3个tick × 3个大周期 × 1次查询）
- **每分钟约 540次查询** ❌ 仍然很密集

### 3. 缓存策略

#### 缓存结构设计

```python
# 1分钟开盘价缓存（已实现）
self._open_price_cache: dict[datetime, float] = {
    datetime(2025, 12, 4, 19, 29): 25950.0,  # 1分钟粒度
    datetime(2025, 12, 4, 19, 30): 25945.0,
    ...
}

# 大周期开盘价缓存（需要实现）
self._large_timeframe_open_price_cache: dict[tuple[Interval, datetime], float] = {
    (Interval.MINUTE_5, datetime(2025, 12, 4, 19, 25)): 25950.0,
    (Interval.HOUR, datetime(2025, 12, 4, 19, 0)): 25900.0,
    (Interval.HOUR_4, datetime(2025, 12, 4, 17, 15)): 25800.0,
    ...
}
```

#### 缓存键设计

**1分钟缓存键**：
```python
cache_key = bar_minute_start  # datetime对象
```

**大周期缓存键**：
```python
cache_key = (interval, large_bar_datetime)  # (Interval, datetime)
```

#### 缓存更新时机

##### 何时写入缓存

**1分钟开盘价**：
- ✅ 第一次查询到正确的开盘价时
- 存储：`self._open_price_cache[bar_minute_start] = correct_open_price`

**大周期开盘价**：
- ✅ 第一次查询到正确的开盘价时
- 存储：`self._large_timeframe_open_price_cache[(interval, large_bar_datetime)] = open_price`

##### 何时失效（清除）缓存

**场景1：合约切换**
```python
def _cleanup_data(self, symbol_changed: bool = True):
    if symbol_changed:
        # 清除所有缓存
        self._open_price_cache.clear()
        self._large_timeframe_open_price_cache.clear()
```

**场景2：数据重新加载**
```python
def _cleanup_data(self, symbol_changed: bool = False):
    # 即使合约未变，重新加载数据时也应清除缓存
    # 因为历史数据范围可能改变
    self._open_price_cache.clear()
    self._large_timeframe_open_price_cache.clear()
```

**场景3：跨日（可选）**
```python
# 如果缓存数据量过大，可以在跨日时清理旧数据
def _cleanup_old_cache(self):
    now = datetime.now()
    today_start = now.replace(hour=0, minute=0, second=0, microsecond=0)
    
    # 清理昨天及之前的缓存
    self._open_price_cache = {
        k: v for k, v in self._open_price_cache.items() 
        if k >= today_start
    }
    self._large_timeframe_open_price_cache = {
        k: v for k, v in self._large_timeframe_open_price_cache.items()
        if k[1] >= today_start  # k[1] 是 datetime
    }
```

##### 缓存不需要更新的情况

**一旦缓存，永远有效**（在同一交易日、同一合约、同一数据集内）：
- 历史K线的开盘价是**固定不变**的
- 例如：19:29分钟的开盘价一旦确定，就不会改变
- 只有在新的一天或切换合约时才需要清除

#### 缓存效果预测

**优化前（无大周期缓存）**：
```
每分钟数据库查询次数：
- 1分钟开盘价：1次（已优化）✅
- 大周期开盘价：540次（180 tick × 3 大周期） ❌
- 合计：541次/分钟
```

**优化后（有大周期缓存）**：
```
每分钟数据库查询次数：
- 1分钟开盘价：1次（已优化）✅
- 5分钟开盘价：1次（每5分钟第一个tick）✅
- 1小时开盘价：1次（每小时第一个tick）✅
- 4小时开盘价：1次（每4小时第一个tick）✅
- 合计：4次/分钟（大部分时候是1次）
```

**性能提升**：
- 减少 **99.3%** 的数据库查询次数 🚀
- 从 **541次/分钟** 降到 **1-4次/分钟**

## 实施优化方案

### 第1步：添加大周期开盘价缓存

```python
# 在 __init__ 中添加
self._large_timeframe_open_price_cache: dict[tuple[Interval, datetime], float] = {}
```

### 第2步：修改 `_get_large_timeframe_open_price()`

```python
def _get_large_timeframe_open_price(
    self,
    large_bar_datetime: datetime,
    interval: Interval,
    current_open_price: float
) -> float:
    # ✅ 检查缓存
    cache_key = (interval, large_bar_datetime)
    if cache_key in self._large_timeframe_open_price_cache:
        return self._large_timeframe_open_price_cache[cache_key]
    
    # 原有查询逻辑...
    open_price = ...  # 从数据库查询
    
    # ✅ 缓存结果
    if open_price:
        self._large_timeframe_open_price_cache[cache_key] = open_price
    
    return open_price
```

### 第3步：清理缓存

```python
def _cleanup_data(self, symbol_changed: bool = True):
    # 清理历史数据...
    
    # ✅ 清理所有缓存
    if hasattr(self, '_open_price_cache'):
        self._open_price_cache.clear()
    if hasattr(self, '_large_timeframe_open_price_cache'):
        self._large_timeframe_open_price_cache.clear()
```

## 总结

### 当前问题

1. **1分钟开盘价查询**：✅ 已优化（缓存）
2. **大周期开盘价查询**：❌ 未优化
   - 每个tick查询3次（5m/1H/4H）
   - 每分钟540次查询（假设180个tick）
   - 严重影响性能

### 优化效果

实施大周期开盘价缓存后：
- **性能提升**：99.3% 的查询次数减少
- **响应速度**：tick处理延迟降低
- **数据库压力**：大幅降低
- **准确性**：完全保持，无任何损失

### 实施优先级

⭐⭐⭐⭐⭐ **非常高**

建议立即实施，因为：
1. 实施简单（类似已有的1分钟缓存）
2. 效果显著（99%+性能提升）
3. 无副作用（历史开盘价不会改变）
4. 用户体验改善明显（实时K线更流畅）

