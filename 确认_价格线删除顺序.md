# 价格线删除顺序确认

## 正确的删除顺序（已修复）

### 原则：先删除关联关系，再删除线本身

**为什么这样做？**
1. ✅ 避免孤儿关联记录（关联关系存在但线已删除）
2. ✅ 数据一致性更好
3. ✅ 清理逻辑更清晰
4. ✅ 符合数据库范式（先删除外键关系，再删除主记录）

---

## 删除入场线的正确顺序（已修复）

**提交**：`c3ece67b` - fix: 调整删除入场线时的顺序，先删除关联关系再删除线

### 修复前（错误顺序）

```python
# ❌ 先删除止损线
stop_loss_line_id = relations.get("stop_loss")
if stop_loss_line_id:
    # 删除止损线...

# ❌ 再删除止盈线
take_profit_line_id = relations.get("take_profit")
if take_profit_line_id:
    # 删除止盈线...

# ❌ 最后才删除关联关系
self._entry_line_relations.pop(line_id, None)
self._price_line_database.delete_relation(line_id)
```

### 修复后（正确顺序）

```python
# ✅ 步骤1：先删除关联关系（内存）
self._entry_line_relations.pop(line_id, None)

# ✅ 步骤2：再删除关联关系（数据库）
if self._price_line_database:
    self._price_line_database.delete_relation(line_id)

# ✅ 步骤3：删除止损线
stop_loss_line_id = relations.get("stop_loss")
if stop_loss_line_id:
    # 删除止损线...

# ✅ 步骤4：删除止盈线
take_profit_line_id = relations.get("take_profit")
if take_profit_line_id:
    # 删除止盈线...

# ✅ 步骤5：删除入场线本身
self._price_line_manager.delete_line(line_id)
```

---

## 删除挂单线的顺序（需要检查）

### 当前实现位置

挂单线的删除可能在以下位置：
1. 用户手动删除挂单线
2. 挂单成交后转换为入场线（原挂单线被删除）
3. 订单取消时删除挂单线

### 需要确认的代码位置

**位置1：挂单成交后转换**（`drawing_order.py`）
```python
# 在挂单成交时，关联关系会被迁移到入场线
# 迁移完成后，应该：
# 1. 先清理挂单线的关联关系（_pending_line_relations）
# 2. 再删除挂单线本身（如果不复用ID）

# 检查 drawing_order.py 第1437-1461行
if line_id != new_line_id:
    # ✅ 先清理数据库中的关联关系
    self._widget._price_line_database.delete_all_relations(line_id)
    
    # 然后（在后续代码中）删除挂单线本身
```

**当前状态**：✅ 顺序正确（先删除关联关系）

---

## 两套关联机制的协调

### 机制1：entry_line_id 属性（PriceLineItem）

```python
stop_loss_line.entry_line_id = "entry_12345"
```

**保存**：
- ✅ `PriceLineData.__init__` 接受 `entry_line_id`
- ✅ `to_dict()` 序列化到JSON
- ✅ 保存到数据库

**加载**：
- ✅ `from_dict()` 从JSON反序列化
- ✅ `load_price_lines()` 恢复到 `PriceLineItem.entry_line_id`

### 机制2：关联关系表（ChartWidget）

```python
self._entry_line_relations["entry_12345"] = {
    "stop_loss": "stop_loss_67890",
    "take_profit": "take_profit_11111"
}
```

**保存**：
- ✅ `_price_line_database.save_relation()` 单个保存
- ✅ 在订单成交时自动保存
- ✅ 在拖拽创建止损/止盈线时保存

**加载**：
- ✅ `_load_line_relations()` 批量加载
- ✅ `get_related_lines()` 单个查询
- ✅ 在 `set_vt_symbol()` 时自动加载

### 删除时的协调

**删除关联关系（两套机制都要清理）**：

```python
# ✅ 方式1：清理关联关系表（内存）
self._entry_line_relations.pop(entry_line_id, None)

# ✅ 方式2：清理关联关系表（数据库）
self._price_line_database.delete_relation(entry_line_id)

# ✅ 方式3：清理属性（通过删除线本身自动清理）
# 当删除止损/止盈线时，其 entry_line_id 属性也随之消失
```

---

## 完整的删除流程检查清单

### ✅ 删除入场线时（已修复 - c3ece67b）

1. ✅ 删除关联关系（内存）：`_entry_line_relations.pop(line_id, None)`
2. ✅ 删除关联关系（数据库）：`delete_relation(line_id)`
3. ✅ 删除关联的止损线：`delete_line(stop_loss_line_id)`
4. ✅ 删除关联的止盈线：`delete_line(take_profit_line_id)`
5. ✅ 删除入场线本身：`delete_line(line_id)`

### ✅ 删除挂单线时（已有正确实现）

1. ✅ 删除关联关系（数据库）：`delete_all_relations(line_id)`
2. ✅ 删除关联关系（内存）：`_pending_line_relations.pop(line_id, None)`
3. （可选）删除挂单线本身：如果复用ID作为入场线，则不删除

### 🔍 需要进一步检查的位置

#### 1. 手动删除挂单线
- 位置：用户在图表上删除挂单线
- 需要确认：是否先删除关联关系

#### 2. 订单取消时删除挂单线
- 位置：订单被取消，自动删除挂单线
- 需要确认：是否先删除关联关系

#### 3. 止损/止盈触发时删除入场线
- 位置：止损/止盈被触发，平仓后删除入场线
- 需要确认：是否先删除关联关系（应该复用已修复的逻辑）

---

## 持久化完整性验证

### 保存时（必须保存的信息）

**PriceLineItem 属性**：
- ✅ line_id, price, line_type, direction
- ✅ vt_orderid
- ✅ entry_line_id ⭐（已修复）
- ✅ created_at

**关联关系表**：
- ✅ _entry_line_relations
- ✅ _pending_line_relations（通过 DrawingOrderController）

### 加载时（必须恢复的信息）

**PriceLineItem 属性**：
- ✅ 所有基本属性
- ✅ entry_line_id ⭐（已修复）

**关联关系表**：
- ✅ _entry_line_relations（通过 _load_line_relations）
- ✅ _pending_line_relations（通过 DrawingOrderController）

**关联到 PriceLineItem**：
- ✅ 调用 `set_associated_entry_line_id()` ⭐（已修复）
- ✅ 激活止损/止盈线（`set_creation_time`）

---

## 总结

### ✅ 已修复的问题

1. **entry_line_id 持久化**（`ab8258e0`, `0b487d24`）
   - PriceLineData 支持 entry_line_id
   - PriceLineItem 有 entry_line_id 属性
   - 保存和加载都正确处理

2. **删除顺序优化**（`c3ece67b`）
   - 删除入场线时先删除关联关系
   - 避免孤儿关联记录

3. **多周期监控修复**（`c38f96da`）
   - 多周期模式下正确监控止损/止盈线

### 🎯 验证建议

**测试场景1：创建并重启**
1. 创建挂单线（带止损/止盈）
2. 模拟成交
3. 关闭程序
4. 重启程序
5. 切换到多周期模式

**预期结果**：
- ✅ 入场线显示
- ✅ 止损线显示 ⭐
- ✅ 止盈线显示 ⭐
- ✅ 关联关系正确

**测试场景2：触发止损**
1. 持仓有止损线
2. 价格突破止损线
3. 观察是否正确平仓
4. 观察是否正确删除所有相关线

**预期结果**：
- ✅ 先删除关联关系
- ✅ 再删除止损/止盈线
- ✅ 最后删除入场线
- ✅ 数据库中无孤儿记录

---

**文档创建时间**：2025-12-04
**相关提交**：c3ece67b, 0b487d24, ab8258e0, c38f96da

