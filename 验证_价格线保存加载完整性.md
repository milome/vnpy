# 价格线保存加载完整性验证

## 双重机制的完整实现

### 机制1：entry_line_id 属性（PriceLineItem层）

**属性定义**：
```python
# PriceLineItem.__init__
self.entry_line_id: Optional[str] = None
```

**设置方法**：
```python
def set_associated_entry_line_id(self, entry_line_id: Optional[str]) -> None:
    self.entry_line_id = entry_line_id
```

### 机制2：关联关系表（ChartWidget层）

**数据结构**：
```python
# ChartWidget
self._entry_line_relations: dict[str, dict[str, str]] = {}
# entry_line_id -> {"stop_loss": stop_loss_line_id, "take_profit": take_profit_line_id}
```

---

## 完整的保存流程

### 场景1：拖拽创建止损/止盈线（widget_mouse.py）

**步骤1：更新关联关系表**
```python
# 第905行 / 第949行
self._entry_line_relations[entry_line_id]["stop_loss"] = new_line_id
# 或
self._entry_line_relations[entry_line_id]["take_profit"] = new_line_id
```

**步骤2：同步设置属性**（✅ 已修复 - ee7f6c62）
```python
# 获取止损/止盈线
new_stop_loss_line = manager.get_line(new_line_id)

# 设置 entry_line_id 属性
if new_stop_loss_line:
    if hasattr(new_stop_loss_line, 'set_associated_entry_line_id'):
        new_stop_loss_line.set_associated_entry_line_id(entry_line_id)
    elif hasattr(new_stop_loss_line, 'entry_line_id'):
        new_stop_loss_line.entry_line_id = entry_line_id
```

**步骤3：保存关联关系到数据库**
```python
# 第907-910行 / 第951-954行
self._price_line_database.save_relation(
    entry_line_id, new_line_id, "stop_loss"  # 或 "take_profit"
)
```

**步骤4：保存价格线属性**（自动触发或手动保存）
```python
# 在save_price_lines()中
entry_line_id = None
if hasattr(line, 'entry_line_id'):
    entry_line_id = line.entry_line_id  # ✅ 从属性获取

line_data = PriceLineData(
    ...,
    entry_line_id=entry_line_id  # ✅ 保存到PriceLineData
)
```

**一致性验证**：
- ✅ `_entry_line_relations[entry_id]["stop_loss"]` = stop_loss_id
- ✅ `stop_loss_line.entry_line_id` = entry_id
- ✅ PriceLineData保存时包含 entry_line_id = entry_id
- ✅ 数据库关联关系记录：(entry_id, stop_loss_id, "stop_loss")

---

### 场景2：挂单成交迁移止损/止盈线（drawing_order.py）

**步骤1：更新关联关系表**
```python
# 第1293行 / 第1375行
self._widget._entry_line_relations[new_line_id]["stop_loss"] = stop_loss_line_id
# 或
self._widget._entry_line_relations[new_line_id]["take_profit"] = take_profit_line_id
```

**步骤2：同步设置属性**（✅ 已修复 - ee7f6c62）
```python
# 设置止损/止盈线的 entry_line_id 属性
if hasattr(stop_loss_line, 'set_associated_entry_line_id'):
    stop_loss_line.set_associated_entry_line_id(new_line_id)
elif hasattr(stop_loss_line, 'entry_line_id'):
    stop_loss_line.entry_line_id = new_line_id
```

**步骤3：保存关联关系到数据库**
```python
# 第1337-1341行 / 第1419-1423行
self._widget._price_line_database.save_relation(
    new_line_id, stop_loss_line_id, "stop_loss"  # 或 "take_profit"
)
```

**步骤4：保存价格线属性**（自动触发）
```python
# 同场景1的步骤4
```

**一致性验证**：
- ✅ `_entry_line_relations[new_line_id]["stop_loss"]` = stop_loss_line_id
- ✅ `stop_loss_line.entry_line_id` = new_line_id
- ✅ PriceLineData保存时包含 entry_line_id = new_line_id
- ✅ 数据库关联关系记录：(new_line_id, stop_loss_line_id, "stop_loss")

---

## 完整的加载流程

### 步骤1：加载价格线（widget_database.py:110-149）

```python
# 从数据库加载价格线
line_data_list = self._price_line_storage.load_lines(self._vt_symbol)

for line_data in line_data_list:
    # 创建价格线
    line_id = self.add_price_line(...)
    
    # ✅ 恢复 entry_line_id 属性
    if line_data.entry_line_id:
        line = self.get_price_line_manager().get_line(line_id)
        if line and hasattr(line, 'entry_line_id'):
            line.entry_line_id = line_data.entry_line_id
```

### 步骤2：加载关联关系表（widget_database.py:17-67）

```python
# 从数据库加载关联关系
all_relations = self._price_line_database.load_relations()

for relation in all_relations:
    entry_line_id = relation["entry_line_id"]
    related_line_id = relation["related_line_id"]
    relation_type = relation["relation_type"]
    
    # ✅ 恢复关联关系表
    if entry_line_id not in self._entry_line_relations:
        self._entry_line_relations[entry_line_id] = {}
    self._entry_line_relations[entry_line_id][relation_type] = related_line_id
    
    # ✅ 同步设置属性
    if hasattr(related_line, 'set_associated_entry_line_id'):
        related_line.set_associated_entry_line_id(entry_line_id)
```

### 步骤3：清理孤儿线（widget.py:425-505）

```python
# 只在加载后清理（避免在加载关联关系前误删）
# 检查两套机制
has_entry = hasattr(line, 'entry_line_id') and line.entry_line_id  # 属性
has_relation = line_id in some_relations  # 关联表

# 两者都没有才删除
if not has_entry and not has_relation:
    # 删除孤儿线
```

**一致性验证**：
- ✅ 属性恢复：`stop_loss_line.entry_line_id` = entry_id
- ✅ 关联表恢复：`_entry_line_relations[entry_id]["stop_loss"]` = stop_loss_id
- ✅ 两套机制完全一致

---

## 一致性保障机制

### 保存时的一致性保障（✅ 已修复）

#### 位置1：拖拽创建（widget_mouse.py）
```python
# 同时更新两套机制
self._entry_line_relations[entry_id]["stop_loss"] = stop_loss_id  # 关联表
stop_loss_line.entry_line_id = entry_id  # ✅ 属性（已添加 - ee7f6c62）

# 保存关联关系
self._price_line_database.save_relation(entry_id, stop_loss_id, "stop_loss")

# 保存价格线属性（自动或手动触发）
# entry_line_id会被保存到PriceLineData
```

#### 位置2：挂单成交迁移（drawing_order.py）
```python
# 同时更新两套机制
self._widget._entry_line_relations[new_id]["stop_loss"] = stop_loss_id  # 关联表
stop_loss_line.entry_line_id = new_id  # ✅ 属性（已添加 - ee7f6c62）

# 保存关联关系
self._widget._price_line_database.save_relation(new_id, stop_loss_id, "stop_loss")

# 保存价格线属性（自动或手动触发）
```

### 加载时的一致性保障（✅ 已有）

#### 步骤1：加载属性
```python
# load_price_lines()
if line_data.entry_line_id:
    line.entry_line_id = line_data.entry_line_id  # ✅ 恢复属性
```

#### 步骤2：加载关联表
```python
# _load_line_relations()
self._entry_line_relations[entry_id][relation_type] = related_id  # ✅ 恢复关联表

# 同步设置属性（双重保障）
related_line.set_associated_entry_line_id(entry_id)  # ✅ 确保属性正确
```

---

## 验证清单

### ✅ 创建时（两套机制同步）

- [x] 更新 `_entry_line_relations` 表
- [x] 设置 `entry_line_id` 属性（✅ ee7f6c62）
- [x] 保存关联关系到数据库
- [x] 保存价格线属性

### ✅ 保存时（完整性）

- [x] 从 `entry_line_id` 属性提取值
- [x] 保存到 `PriceLineData`
- [x] 序列化到JSON
- [x] 保存关联关系表到数据库

### ✅ 加载时（完整性）

- [x] 从JSON反序列化
- [x] 恢复到 `entry_line_id` 属性
- [x] 加载关联关系表
- [x] 同步设置属性（双重保障）

### ✅ 删除时（顺序正确）

- [x] 先删除关联关系（内存 + 数据库）（✅ c3ece67b）
- [x] 再删除关联的线
- [x] 最后删除主线

---

## 相关提交

### 完整修复链（共5个提交）

1. **ab8258e0** - entry_line_id 持久化（PriceLineData层）
2. **0b487d24** - entry_line_id 属性（PriceLineItem层）
3. **c3ece67b** - 删除顺序优化
4. **c38f96da** - 多周期监控修复
5. **ee7f6c62** - 属性和关联表同步更新 ⭐

---

## 测试验证步骤

### 测试1：创建并重启

1. 创建挂单线（带止损/止盈）
2. 模拟成交
3. **验证**：
   - `stop_loss_line.entry_line_id` 应该 = entry_line_id ✅
   - `_entry_line_relations[entry_id]["stop_loss"]` 应该 = stop_loss_id ✅
4. 关闭程序
5. 重启程序
6. **验证**：
   - 入场线显示 ✅
   - 止损线显示 ✅
   - 止盈线显示 ✅
   - 止损/止盈监控正常工作 ✅

### 测试2：手动创建止损/止盈线

1. 创建入场线（或已有入场线）
2. 拖拽创建止损线
3. **验证**：
   - `stop_loss_line.entry_line_id` 应该 = entry_line_id ✅
   - `_entry_line_relations[entry_id]["stop_loss"]` 应该 = stop_loss_id ✅
   - 数据库中有关联关系记录 ✅
4. 拖拽创建止盈线
5. **验证**（同上）
6. 重启程序
7. **验证**：
   - 所有线都正确显示 ✅
   - 关联关系完整 ✅

### 测试3：多周期模式

1. 单周期创建持仓（带止损/止盈）
2. 切换到多周期模式
3. **验证**：
   - 入场线显示 ✅
   - 止损线显示 ✅
   - 止盈线显示 ✅
4. 价格突破止损线
5. **验证**：
   - 正确触发平仓 ✅
   - 所有相关线被删除 ✅

---

## 保障机制总结

### 双重保障

**创建/更新时**：
1. 更新关联关系表 → `_entry_line_relations[entry_id]["stop_loss"] = stop_loss_id`
2. 设置属性 → `stop_loss_line.entry_line_id = entry_id` ⭐
3. 保存关联关系到数据库 → `save_relation(entry_id, stop_loss_id, "stop_loss")`
4. 保存价格线属性 → PriceLineData包含entry_line_id

**加载时**：
1. 加载价格线属性 → `line.entry_line_id = line_data.entry_line_id`
2. 加载关联关系表 → `_entry_line_relations[entry_id][type] = related_id`
3. 双重同步 → `related_line.set_associated_entry_line_id(entry_id)`

**检查时**：
1. 检查属性 → `has_entry = hasattr(line, 'entry_line_id') and line.entry_line_id`
2. 检查关联表 → `has_relation = line_id in some_relations`
3. 双重检查 → 两者都满足才认为有关联

### 三层防护

**Layer 1：内存层**
- PriceLineItem.entry_line_id 属性
- ChartWidget._entry_line_relations 字典

**Layer 2：持久化层**
- PriceLineData.entry_line_id字段
- 数据库关联关系表

**Layer 3：同步机制**
- 创建/更新时同步设置两套机制（✅ ee7f6c62）
- 加载时双重恢复两套机制
- 检查时交叉验证两套机制

---

## 结论

### ✅ 已确保保存流程的一致性

**提交 ee7f6c62 修复后**：

1. **拖拽创建止损/止盈线时**：
   - ✅ 关联关系表更新
   - ✅ entry_line_id属性同步设置
   - ✅ 保存到数据库（关联关系 + 属性）

2. **挂单成交迁移时**：
   - ✅ 关联关系表更新
   - ✅ entry_line_id属性同步设置
   - ✅ 保存到数据库（关联关系 + 属性）

3. **重启后加载**：
   - ✅ entry_line_id属性恢复
   - ✅ 关联关系表恢复
   - ✅ 双重同步确保一致

4. **运行时检查**：
   - ✅ 两套机制交叉验证
   - ✅ 孤儿线准确识别
   - ✅ 数据完整性有保障

---

**文档创建时间**：2025-12-04
**相关提交**：ee7f6c62, c3ece67b, 0b487d24, ab8258e0, c38f96da

