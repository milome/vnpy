# DataManager自动刷新问题分析

## 问题描述

1. **刷新功能无效**：DataManager的"刷新"按钮虽然可以刷新UI显示，但不会自动检测数据库中的新数据
2. **缺少定时更新**：DataRecorder每10秒写入数据到数据库，但DataManager没有定时刷新机制
3. **用户体验差**：用户必须手动点击"更新数据"按钮才能看到最新数据

## 当前实现分析

### 刷新功能 (`refresh_tree()`)
- 重新查询 `engine.get_bar_overview()` 获取数据概览
- 清空并重建树形控件
- **问题**：只是重新查询数据库，不会自动定时执行

### 更新数据功能 (`update_data()`)
- 从数据库中已有数据的结束日期开始，下载到当前最新日期的数据
- 会下载并合成新数据
- **问题**：需要手动点击，不会自动执行

## 解决方案

### 方案1：添加定时自动刷新功能（推荐）

在DataManager UI中添加定时器，定期自动刷新数据列表：

1. **添加QTimer定时器**
   - 默认间隔：30秒（可配置）
   - 自动调用 `refresh_tree()` 刷新UI

2. **添加自动刷新开关**
   - 复选框或按钮，允许用户开启/关闭自动刷新
   - 默认开启自动刷新

3. **优化刷新性能**
   - 只刷新变化的部分，而不是重建整个树
   - 或者添加刷新状态提示

### 方案2：监听数据库变化事件

1. **监听DataRecorder的数据保存事件**
   - DataRecorder保存数据后发送事件
   - DataManager监听事件并自动刷新

2. **问题**：需要修改事件机制，可能影响其他模块

### 方案3：智能刷新机制

1. **检测数据变化**
   - 记录上次刷新的时间戳
   - 定时检查数据库中的数据是否有变化
   - 有变化时才刷新UI

2. **性能考虑**
   - 避免频繁查询数据库
   - 可以只在用户切换到DataManager窗口时检查

## 推荐实现：方案1

添加定时自动刷新功能，实现简单、用户体验好。

