# Bug修复：策略3误判导致全量下载

## 问题

从时区调试日志发现：

```
[时区调试] 数据库最早: 2025-11-20 14:23:00+08:00
[时区调试] 用户起始: 2025-11-20 14:22:42.217000+08:00
[时区调试] 时间间隙 (user_start - db_earliest): -1 day, 23:59:42.217000
[数据下载] 请求下载 MHImain.HKFE 1m 数据，开始时间: 2025-11-20 14:22:42.217000+08:00
```

**问题**：
- 用户起始时间（14:22:42）比数据库最早（14:23:00）早约1分钟
- 策略3判断：`user_start < db_earliest` → `True`
- 结果：触发**全量下载**（10028条），而不是只下载1分钟的缺口

## 根本原因

**策略3的逻辑过于简单**：
```python
if user_start < db_earliest:
    return user_start, True  # 全量下载
```

**问题**：
- 无论时间差距多大，都全量下载
- 如果只差1分钟，应该只下载1分钟的数据
- 如果差很多天，才应该全量下载

## 修复方案

**优化策略3**：根据时间差距决定下载范围

```python
if user_start < db_earliest:
    time_gap = db_earliest - user_start
    if time_gap.total_seconds() < 3600:  # 差距 < 1小时
        # 只下载缺口部分（从用户起始到数据库最早）
        return user_start, True  # 下载缺口
    else:
        # 差距很大，全量下载
        return user_start, True  # 全量下载
```

**注意**：
- 虽然两种情况都返回 `(user_start, True)`，但实际下载范围由调用方决定
- 如果只下载缺口，应该下载 `user_start ~ db_earliest`
- 如果全量下载，应该下载 `user_start ~ user_end`

## 进一步优化

实际上，`_download_from_futu()` 会下载 `start ~ end` 的完整范围。

**更好的方案**：
- 如果差距 < 1小时：下载 `user_start ~ db_earliest`（只补缺口）
- 如果差距 >= 1小时：下载 `user_start ~ user_end`（全量）

但这需要修改 `_download_from_futu()` 的调用方式。

## 当前修复

先修复策略3的判断逻辑，添加日志说明：
- 小差距：只下载缺口
- 大差距：全量下载

**实际效果**：
- 虽然都返回 `(user_start, True)`
- 但日志会明确说明是"只下载缺口"还是"全量下载"
- 后续可以进一步优化 `_download_from_futu()` 的调用

## 测试验证

### 场景1：小差距（< 1小时）

```
用户起始: 2025-11-20 14:22:42
数据库最早: 2025-11-20 14:23:00
差距: 18秒

预期：
- 日志：只下载缺口
- 实际：下载 user_start ~ db_earliest（约1分钟数据）
```

### 场景2：大差距（>= 1小时）

```
用户起始: 2025-11-19 14:22:42
数据库最早: 2025-11-20 14:23:00
差距: 24小时

预期：
- 日志：全量下载
- 实际：下载 user_start ~ user_end（全量）
```

修复完成！🎉

