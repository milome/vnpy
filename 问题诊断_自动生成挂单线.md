# 问题诊断：多周期下自动生成挂单线

## 问题

多周期模式下，刚开启画线下单功能，还没有画线，就自动显示了挂单止损线和挂单止盈线。

## 可能原因

### 原因1：从数据库加载了旧的挂单线

**猜测**：
- 切换到多周期模式时，调用了 `load_all_lines()`
- `load_all_lines()` 从数据库加载了之前保存的挂单止损止盈线
- 这些线显示在图表上

**验证方法**：
- 检查 `_switch_to_multi_timeframe_mode()` 是否调用了 `load_all_lines()`
- 检查数据库中是否有旧的挂单线记录

### 原因2：从单周期同步了价格线

**猜测**：
- 切换到多周期模式时，同步了单周期图表上的价格线
- 单周期图表上有挂单止损止盈线
- 这些线被复制到多周期图表上

**验证方法**：
- 检查 `_switch_to_multi_timeframe_mode()` 是否有同步价格线的逻辑

### 原因3：启用画线下单时自动创建

**猜测**：
- 调用 `enable_drawing_order()` 时，自动创建了挂单止损止盈线
- 或者在某个初始化逻辑中自动创建

**验证方法**：
- 检查 `enable_drawing_order()` 的实现
- 检查是否有自动创建挂单线的逻辑

## 排查步骤

### 1. 检查切换模式时的逻辑

```python
def _switch_to_multi_timeframe_mode(self):
    # ... 切换逻辑 ...
    
    # 检查是否调用了这个：
    self.multi_timeframe_widget._chart.get_price_line_manager().load_all_lines()
```

### 2. 检查数据库中的价格线

```sql
SELECT * FROM price_lines 
WHERE vt_symbol = 'MHImain.HKFE' 
AND line_type IN ('stop_loss', 'take_profit')
```

### 3. 检查价格线的创建时间和类型

- 挂单止损止盈线：通常在开启画线下单前就存在
- 入场线关联的止损止盈线：在模拟成交后创建

## 临时解决方案

### 方案1：清空数据库中的旧挂单线

```python
# 在切换到多周期模式前，清空数据库中的挂单线
def _switch_to_multi_timeframe_mode(self):
    # 清空旧的挂单线
    if self.multi_timeframe_widget._chart._price_line_database:
        db = self.multi_timeframe_widget._chart._price_line_database
        # 删除所有挂单止损止盈线
        # ... 删除逻辑 ...
```

### 方案2：不自动加载挂单线

```python
# 修改 load_all_lines() 的调用
# 只加载入场线和已激活的止损止盈线
# 不加载挂单止损止盈线（未关联到入场线的）
```

### 方案3：启用画线下单时清空旧的挂单线

```python
def toggle_drawing_mode(self):
    if not drawing_enabled:  # 如果是启用画线下单
        # 清空旧的挂单线
        chart = self.multi_timeframe_widget._chart if self.display_mode == "多周期叠加" else self.chart
        manager = chart.get_price_line_manager()
        
        # 删除所有未关联到入场线的止损止盈线
        all_lines = manager.get_all_lines()
        for line_id, line in all_lines.items():
            if line.get_line_type() in [PriceLineType.STOP_LOSS, PriceLineType.TAKE_PROFIT]:
                # 检查是否有 entry_line_id
                if not hasattr(line, 'entry_line_id') or not line.entry_line_id:
                    # 这是挂单线，删除
                    manager.remove_line(line_id)
```

## 需要更多信息

请提供以下信息帮助诊断：

1. **这些挂单线是什么时候创建的？**
   - 之前使用画线下单功能时创建的？
   - 还是系统自动创建的？

2. **这些挂单线的ID是什么？**
   - 例如：`pending_stop_xxx` 或 `stop_xxx`

3. **单周期模式下是否也有这些挂单线？**
   - 如果单周期有，可能是从单周期同步的
   - 如果单周期没有，可能是数据库中的旧数据

4. **关闭画线下单后，这些挂单线是否消失？**
   - 如果消失，说明是启用画线下单时加载的
   - 如果不消失，说明是其他逻辑创建的

## 下一步

请提供更多日志或截图，帮助确定这些挂单线的来源。

我会根据具体情况实施修复。

