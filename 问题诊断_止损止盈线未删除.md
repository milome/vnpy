# 问题诊断：止损止盈线未删除

## 问题

模拟止盈后，平仓成功，入场线被删除，但关联的止损止盈线仍然显示。

## 日志分析

```
2025-12-04 06:03:56.759 | INFO | Chart | [持仓同步] 标记删除入场线: entry_recovered_c377ded5 (方向=long, 匹配=True)
2025-12-04 06:03:56.759 | INFO | Chart | [持仓同步] 找到 1 条long方向的入场线待删除
2025-12-04 06:03:56.759 | INFO | Chart | [持仓同步] 已从plot中移除入场线: entry_recovered_c377ded5
2025-12-04 06:03:56.765 | INFO | Chart | [持仓同步] 已删除入场线: entry_recovered_c377ded5
2025-12-04 06:03:56.766 | INFO | Chart | [持仓同步] 持仓为0，已删除 1 条long方向的价格线（包括入场线及关联的止损/止盈线，共找到 1 条入场线）
```

**问题**：
- 日志说"包括入场线及关联的止损/止盈线"
- 但实际上止损线 `stop_3bbaefb94cba` 和止盈线 `profit_61194a489042` 没有被删除
- 只删除了入场线 `entry_recovered_c377ded5`

## 可能原因

### 原因1：入场线和止损止盈线的关联关系丢失

止损止盈线可能没有正确关联到入场线，所以删除入场线时找不到关联的止损止盈线。

**检查点**：
- 止损线和止盈线创建时是否设置了 `entry_line_id`
- 入场线 ID 是 `entry_recovered_c377ded5`（恢复的入场线）
- 止损线 ID 是 `stop_3bbaefb94cba`
- 止盈线 ID 是 `profit_61194a489042`

### 原因2：删除逻辑有问题

删除入场线时的代码可能没有正确查找和删除关联的止损止盈线。

**需要检查**：
- 删除入场线的代码逻辑
- 如何查找关联的止损止盈线
- 是否正确删除了关联的线

### 原因3：多周期模式的特殊情况

在多周期模式下，可能有两个图表（单周期和多周期），止损止盈线可能在另一个图表上。

**日志显示**：
```
2025-12-04 06:03:55.587 | INFO | MainEngine | [模拟止盈] 使用多周期图表
```

所以是在多周期图表上操作的。

## 排查步骤

### 1. 检查入场线创建时的关联设置

入场线ID是 `entry_recovered_c377ded5`，这是一个"恢复"的入场线，可能在恢复时没有正确设置关联关系。

### 2. 检查止损止盈线的 `entry_line_id` 属性

```python
# 应该检查止损线和止盈线的 entry_line_id 是否正确设置
stop_line = price_line_manager.get_line('stop_3bbaefb94cba')
profit_line = price_line_manager.get_line('profit_61194a489042')

# 检查它们的 entry_line_id 是否 == 'entry_recovered_c377ded5'
```

### 3. 检查删除逻辑

```python
# 删除入场线时，应该：
# 1. 找到所有关联的止损止盈线
associated_lines = []
for line_id, line in all_lines.items():
    if hasattr(line, 'entry_line_id') and line.entry_line_id == entry_line_id:
        associated_lines.append(line_id)

# 2. 删除所有关联的线
for line_id in associated_lines:
    price_line_manager.remove_line(line_id)
```

## 临时解决方案

**手动删除止损止盈线**：

在删除入场线后，额外检查并删除所有止损止盈线：

```python
# 伪代码
def delete_entry_line_and_associated_lines(entry_line_id, direction):
    # 1. 删除入场线
    price_line_manager.remove_line(entry_line_id)
    
    # 2. 查找所有关联的止损止盈线
    all_lines = price_line_manager.get_all_lines()
    lines_to_delete = []
    
    for line_id, line in all_lines.items():
        # 检查是否有 entry_line_id 属性
        if hasattr(line, 'entry_line_id'):
            if line.entry_line_id == entry_line_id:
                lines_to_delete.append(line_id)
        # 或者根据方向删除
        elif line.get_line_type() in [PriceLineType.STOP_LOSS, PriceLineType.TAKE_PROFIT]:
            if line.direction == direction:
                lines_to_delete.append(line_id)
    
    # 3. 删除所有关联的线
    for line_id in lines_to_delete:
        price_line_manager.remove_line(line_id)
        logger.info(f"[持仓同步] 已删除关联的止损/止盈线: {line_id}")
```

## 需要查找的代码位置

1. **入场线恢复逻辑**：`entry_recovered_` 前缀的入场线是如何创建的
2. **持仓同步逻辑**：持仓为0时删除入场线的代码
3. **价格线关联逻辑**：止损止盈线如何关联到入场线

## 下一步

需要查看具体的代码实现，找到删除入场线时没有删除关联止损止盈线的原因。

