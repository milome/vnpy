# 智能追价重复成交问题分析

**日期**: 2025-12-04  
**问题**: 智能追价重试导致持仓翻倍

## 问题现象

### 用户操作
- 多单挂单1手，智能追价（Retry2）

### 实际结果
- 持仓2手（应该1手）

### 关键日志时序

```
22:06:20.337 - 下单 6463050（对手价+智能追价）
22:06:20.471 - 挂单线 pending_63935dc4bc77 关联到订单 FUTU.6463050

22:06:24.448 - 订单 6463050 超时3.5秒，撤单成功
22:06:24.449 - 重试订单 6463051（第1次重试）

22:06:25.987 - 恢复入场线 entry_recovered_f3fde0d2（持仓1手）⚠️
22:06:26.005 - 恢复入场线 entry_recovered_979ff01e（持仓1手）⚠️
              ^^^^ 此时持仓已经是2手了！说明6463050实际上成交了

22:06:28.446 - 订单 6463051 超时3.5秒，撤单成功
22:06:28.447 - 重试订单 6463052（第2次重试）

22:06:29.070 - 订单 6463052 完全成交
22:06:29.074 - 订单 6463052 未关联到任何挂单线
22:06:29.075 - 挂单线 pending_63935dc4bc77 关联的订单 6463050 已被撤销，清理映射关系
22:06:29.075 - 匹配挂单线 pending_63935dc4bc77 -> 订单 6463052
22:06:29.092 - 添加入场线 entry_7a2bcd43f9aa（订单 6463052）

结果：持仓2手（6463050 + 6463052 都成交了）
```

## 问题根因

### 1. FUTU API的行为
- **撤单API返回成功**，但实际上订单可能**已经成交**
- **成交消息延迟推送**（可能晚于撤单响应）
- 时间窗口：撤单响应到成交推送之间可能有几秒延迟

### 2. 时序分析

```
T0: 下单 6463050
T1: 3.5秒超时，调用撤单API
T2: 撤单API返回成功（但实际上订单已在交易所成交，只是消息还没推送）
T3: 立即重试，下单 6463051
T4: FUTU延迟推送6463050的成交消息（持仓+1）
T5: 6463051超时，撤单，重试6463052
T6: 6463052成交（持仓+1）
结果：持仓+2（重复）
```

### 3. 为什么0.1秒等待不够？

- FUTU成交消息推送延迟可能**远大于0.1秒**（从日志看可能1-5秒）
- 增加等待时间会**降低智能追价的速度**，违背设计初衷

## 解决方案

### ❌ 方案A：增加等待时间（已否决）
- 增加撤单后的等待时间（如0.5-1秒）
- **缺点**：降低智能追价速度，违背设计初衷

### ✅ 方案B：在DrawingOrderController层面检测重复（推荐）

#### 核心思路
**不修改智能追价的速度，而是在订单成交后检测和处理重复持仓**

#### 实现方案

1. **在订单成交时，检查是否有其他订单也成交了**
   - 订单6463052成交时，检查原始订单6463050是否也成交
   - 通过 `original_orderid` 追踪

2. **检测同一挂单线关联的多个订单都成交**
   - 同一个挂单线（如 `pending_63935dc4bc77`）
   - 关联了多个订单（6463050, 6463051, 6463052）
   - 如果多个订单都成交，只保留最后一个

3. **在持仓同步时检测异常**
   - 如果实际持仓 > 持仓记录总手数
   - 检查是否有重复的入场线（价格相同、方向相同、时间接近）
   - 自动删除重复的入场线

#### 实现位置

```python
# vnpy/chart/drawing_order.py

def on_order(self, order: OrderData) -> bool:
    """处理订单事件"""
    if order.status == Status.ALLTRADED:
        # ✅ 检测重复成交：同一挂单线的多个重试订单都成交
        pending_line_id = self._find_pending_line_for_order(order)
        if pending_line_id:
            # 检查是否有其他订单也关联到这个挂单线并且已经成交
            duplicate_orders = self._check_duplicate_filled_orders(pending_line_id, order.vt_orderid)
            if duplicate_orders:
                self.write_log(
                    f"⚠️ 检测到重复成交：挂单线 {pending_line_id} 关联的多个订单都成交了：{duplicate_orders + [order.vt_orderid]}"
                )
                # 只保留最后成交的订单，忽略之前的
                # 不创建入场线，记录日志
                return False
        
        # 继续正常处理...
```

### ✅ 方案C：在FUTU Gateway层面追踪重试订单（补充）

#### 实现思路
在 `ChaseOrder` 中记录所有重试订单ID，成交时检查是否有其他订单也成交

```python
class ChaseOrder:
    def __init__(self, ...):
        self.original_orderid = orderid  # 首次下单ID
        self.retry_orderids: List[str] = []  # 所有重试订单ID
        ...

# 重试时记录
chase_order.retry_orderids.append(new_orderid)

# 成交时检查
def on_order_update(self, order: OrderData):
    if order.status == Status.ALLTRADED:
        chase_order = self._safe_get_chase_order(orderid)
        if chase_order:
            # 检查是否有其他重试订单也成交了
            for retry_id in chase_order.retry_orderids:
                if retry_id != orderid:
                    retry_status = self._get_order_status(retry_id)
                    if retry_status == Status.ALLTRADED:
                        self.write_log(f"⚠️ 检测到重复成交：{chase_order.original_orderid} 的多个重试订单都成交了")
                        # 触发警告事件，通知上层处理
```

## 最佳实践建议

### 1. 短期方案（推荐）
**方案B：在DrawingOrderController层面检测重复**
- 不影响智能追价速度
- 在订单成交后检测和清理重复
- 实现简单，风险低

### 2. 长期方案
**方案B + 方案C 组合**
- Gateway层面：追踪所有重试订单，检测重复成交
- Controller层面：作为第二道防线，清理重复入场线
- 双重保护，确保不会出现重复持仓

### 3. 监控和日志
- 记录所有重复成交事件
- 统计重复成交频率
- 如果频繁发生，考虑调整撤单策略

## 测试建议

### 模拟场景
1. 挂单触发下单
2. 订单超时，撤单并重试
3. 原订单延迟成交（模拟FUTU延迟推送）
4. 重试订单也成交
5. **期望**：检测到重复，只保留一个入场线，持仓正确

### 验证要点
- 持仓手数正确（1手，不是2手）
- 只有一条入场线
- 日志中有重复检测的警告

## 总结

**不要修改智能追价的速度逻辑**，而是在订单成交后检测和处理重复持仓。这样既保持了智能追价的快速成交特性，又避免了重复持仓的问题。

