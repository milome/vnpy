# 问题诊断：挂单止损止盈线消失

## 问题

多周期模式下，挂单模拟成交后：
- ✅ 入场线出现了
- ❌ 挂单止损线消失了
- ❌ 挂单止盈线消失了

## 可能原因

### 原因1：清理逻辑误删了挂单止损止盈线

在 `_cleanup_pending_stop_profit_lines()` 中：

```python
# 清理未关联到入场线的止损止盈线
if line_type in [STOP_LOSS, TAKE_PROFIT]:
    if not hasattr(line, 'entry_line_id') or not line.entry_line_id:
        # ❌ 问题：挂单止损止盈线此时还没有 entry_line_id
        # 在挂单成交转为入场线时，才会设置 entry_line_id
        lines_to_remove.append(line_id)
```

**时序问题**：
1. 启用画线下单 → 创建挂单止损止盈线（entry_line_id = None）
2. 用户画线下单 → 创建挂单线，关联到挂单止损止盈线
3. 切换到多周期 → `_cleanup_pending_stop_profit_lines()` 删除了挂单止损止盈线 ❌
4. 挂单成交 → 入场线出现，但止损止盈线已经被删除了

### 原因2：挂单止损止盈线没有正确关联到挂单线

挂单止损止盈线应该通过某种方式关联到挂单线，而不是直接关联到入场线。

**检查点**：
- 挂单线（PENDING）的ID是什么？
- 挂单止损止盈线如何关联到挂单线？
- 挂单成交时，挂单止损止盈线如何转换为已激活的止损止盈线？

### 原因3：挂单成交时没有激活止损止盈线

挂单成交后的逻辑：

```python
# 应该做的事：
1. 挂单线（PENDING）→ 入场线（ENTRY）
2. 挂单止损线（entry_line_id=None）→ 已激活止损线（entry_line_id=入场线ID）
3. 挂单止盈线（entry_line_id=None）→ 已激活止盈线（entry_line_id=入场线ID）

# 可能实际发生的事：
1. 挂单线 → 入场线 ✅
2. 挂单止损线没有更新 ❌
3. 挂单止盈线没有更新 ❌
```

## 排查步骤

### 1. 检查清理逻辑的时机

**问题**：清理逻辑在 `set_vt_symbol()` 中执行，这可能发生在挂单创建之后

**解决方案**：清理逻辑应该只在特定条件下执行，不应该误删正在使用的挂单止损止盈线

```python
def _cleanup_pending_stop_profit_lines(self):
    """清理未关联的挂单止损止盈线"""
    
    # ✅ 改进：检查是否有关联的挂单线
    for line_id, line in all_lines.items():
        if line_type in [STOP_LOSS, TAKE_PROFIT]:
            if not line.entry_line_id:
                # 检查是否有关联的挂单线（PENDING）
                has_associated_pending = False
                for other_id, other_line in all_lines.items():
                    if other_line.get_line_type() == PriceLineType.PENDING:
                        # 检查挂单线是否关联到这个止损止盈线
                        # ... 检查逻辑 ...
                        has_associated_pending = True
                        break
                
                if not has_associated_pending:
                    # 真正的孤儿线，可以删除
                    lines_to_remove.append(line_id)
```

### 2. 检查挂单成交时的激活逻辑

**需要检查**：
- 挂单成交后，是否调用了激活止损止盈线的逻辑
- 是否正确设置了 `entry_line_id`

### 3. 检查挂单线和止损止盈线的关联关系

**需要检查**：
- 挂单线如何关联到挂单止损止盈线
- 这个关联关系是否在 `_entry_line_relations` 中
- 还是有其他的关联机制（例如 `pending_line_id`）

## 临时解决方案

### 方案1：暂时禁用清理逻辑

```python
def _cleanup_pending_stop_profit_lines(self):
    """暂时禁用，避免误删"""
    return  # 临时禁用
```

### 方案2：只清理真正的孤儿线

```python
def _cleanup_pending_stop_profit_lines(self):
    """只清理没有任何关联的止损止盈线"""
    
    # 收集所有入场线ID和挂单线ID
    entry_line_ids = set()
    pending_line_ids = set()
    
    for line_id, line in all_lines.items():
        if line.get_line_type() == PriceLineType.ENTRY:
            entry_line_ids.add(line_id)
        elif line.get_line_type() == PriceLineType.PENDING:
            pending_line_ids.add(line_id)
    
    # 只删除既不关联入场线也不关联挂单线的止损止盈线
    for line_id, line in all_lines.items():
        if line_type in [STOP_LOSS, TAKE_PROFIT]:
            has_entry = hasattr(line, 'entry_line_id') and line.entry_line_id
            has_pending = hasattr(line, 'pending_line_id') and line.pending_line_id
            
            if not has_entry and not has_pending:
                # 真正的孤儿线
                lines_to_remove.append(line_id)
```

## 需要更多信息

请提供以下日志：

1. **挂单创建时的日志**：
   - 挂单线ID
   - 挂单止损线ID
   - 挂单止盈线ID

2. **挂单成交时的日志**：
   - 挂单线如何转换为入场线
   - 止损止盈线是否被激活
   - entry_line_id 是否被设置

3. **清理时的日志**：
   - 哪些线被清理了
   - 清理的原因

这样我可以更准确地定位问题。

