# 诊断：隐藏挂单线导致重复开仓

## 用户假设

**有没有可能有一条之前的挂单线（不显示）也触发开仓了？**

## 可能的场景

### 场景1：数据库中的残留挂单线
1. 用户之前画了挂单线A，保存到数据库
2. 用户删除了挂单线A（UI上不显示）
3. **但价格突破监控（BreakthroughMonitor）还在监控挂单线A**
4. 价格触及，挂单线A触发下单并成交
5. 用户又画了新的挂单线B，也触发并成交
6. 结果：2手持仓

### 场景2：切换合约后的残留
1. 用户在合约X画了挂单线
2. 切换到合约Y
3. 挂单线应该被清理，但可能没有完全清理
4. 切换回合约X时，挂单线还在监控中
5. 导致重复触发

### 场景3：模拟成交后的残留
1. 用户模拟成交挂单线A
2. 挂单线A应该转换为入场线
3. **但如果转换失败，挂单线A可能还在监控中**
4. 用户又画了新的挂单线B
5. 价格触及，两个挂单线都触发

## 诊断方法

### 1. 检查日志中的订单来源

从你的日志中查找：

```bash
# 搜索所有挂单触发的日志
grep "挂单触发" 日志文件

# 搜索所有订单关联的日志
grep "挂单线.*关联到订单" 日志文件

# 搜索所有价格突破的日志
grep "价格突破" 日志文件
```

**关键问题**：
- 在22:06:20下单6463050之前，是否还有其他订单？
- 恢复的2条入场线（entry_recovered_f3fde0d2 和 entry_recovered_979ff01e）对应的订单ID是什么？

### 2. 检查BreakthroughMonitor中的挂单线

在日志中查找：

```bash
# 搜索注册价格突破监控的日志
grep "注册价格突破监控\|register_line" 日志文件

# 搜索挂单线清理的日志
grep "清理挂单线\|unregister_line" 日志文件
```

**关键问题**：
- 在22:06:20之前，BreakthroughMonitor中有几条挂单线？
- 这些挂单线的ID是什么？

### 3. 检查数据库中的挂单线

```python
# 在程序中添加调试代码
def check_pending_lines(self):
    all_lines = self._price_line_manager.get_all_lines()
    pending_lines = [
        (line_id, line.get_price(), line.get_direction())
        for line_id, line in all_lines.items()
        if line.get_line_type() == PriceLineType.PENDING
    ]
    self.write_log(f"当前挂单线: {pending_lines}")
```

### 4. 检查持仓恢复逻辑

从日志中的"恢复入场线"来看：

```
22:06:25.987 | INFO | DrawingOrderController | [DrawingOrderController] 已添加入场线 entry_recovered_f3fde0d2 到图表，价格=25941.0，方向=long
22:06:26.005 | INFO | DrawingOrderController | [DrawingOrderController] 已添加入场线 entry_recovered_979ff01e 到图表，价格=25941.0，方向=long
```

**关键问题**：
- 这两条入场线是因为什么订单而恢复的？
- 它们对应的vt_orderid是什么？

## 需要的日志

请提供从**22:05:00到22:07:00**的完整日志，特别关注：

### 1. 所有挂单相关的日志
```
grep -E "(挂单|pending|PENDING)" 日志文件 | grep "22:0[5-7]:"
```

### 2. 所有订单相关的日志
```
grep -E "(订单|order|委托)" 日志文件 | grep "22:0[5-7]:" | grep -E "(6463050|6463051|6463052)"
```

### 3. 所有持仓同步的日志
```
grep "持仓同步\|恢复入场线" 日志文件 | grep "22:0[5-7]:"
```

### 4. 所有价格突破的日志
```
grep "价格突破\|breakthrough" 日志文件 | grep "22:0[5-7]:"
```

## 可能的Bug位置

### 1. 挂单线删除不完整

```python
# vnpy/chart/widget.py or drawing_order.py

def delete_pending_line(self, line_id: str):
    # ⚠️ 可能的问题：只删除了UI，没有从 BreakthroughMonitor 中注销
    self._price_line_manager.delete_line(line_id)
    
    # ✅ 应该同时注销监控
    if self._breakthrough_monitor:
        self._breakthrough_monitor.unregister_line(line_id)
```

### 2. 模拟成交后的清理不完整

```python
# vnpy/chart/drawing_order.py

def simulate_pending_fill(self, line_id: str):
    # ⚠️ 可能的问题：转换为入场线后，挂单线还在监控中
    entry_line_id = self._convert_pending_to_entry(line_id)
    
    # ✅ 应该从监控中移除挂单线
    if self._widget._breakthrough_monitor:
        self._widget._breakthrough_monitor.unregister_line(line_id)
```

### 3. 切换合约时的清理不完整

```python
# vnpy/chart/widget.py

def set_vt_symbol(self, vt_symbol: str):
    # ⚠️ 可能的问题：切换合约时，只清理了UI，没有清理监控
    self._price_line_manager.clear_all()
    
    # ✅ 应该同时清理监控
    if self._breakthrough_monitor:
        self._breakthrough_monitor.clear()
```

## 建议的检查步骤

1. **立即检查**：在UI上点击"刷新"，看实际显示的挂单线有几条
2. **检查数据库**：查看price_lines表，看是否有多条PENDING类型的线
3. **检查BreakthroughMonitor**：添加日志输出当前监控的所有线
4. **重现问题**：清空所有挂单线，只画一条，看是否还会重复

## 下一步

请提供上述日志，我们可以精确定位是哪条"幽灵挂单线"导致的问题。

