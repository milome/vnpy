# ✅ 实现完成：多周期数据下载功能

## 实现方案

### 复用现有功能

**方案1**（优先）：使用 DataManager 下载
```python
datamanager_engine = main_engine.engines.get("DataManager")
if datamanager_engine:
    datamanager_engine.download_history_data(
        symbol=symbol,
        exchange=exchange,
        interval=Interval.MINUTE,
        start=start,
        end=end
    )
```

**方案2**（备用）：使用 FUTU Gateway 直接下载
```python
futu_gateway = main_engine.gateways["FUTU"]
req = HistoryRequest(symbol, exchange, Interval.MINUTE, start, end)
bars = futu_gateway.query_history(req)
database.save_bar_data(bars)
```

### 下载策略

#### 情况1：用户选择时间早于数据库最早时间

```
用户选择: 2024-01-01
数据库最早: 2025-09-01

判断: 数据库数据不全
策略: 从 FUTU API 全量下载 (2024-01-01 ~ 2025-12-04)
```

**流程**：
1. 判断数据不全
2. 调用 DataManager.download_history_data() 下载
3. 保存到数据库
4. 从数据库重新加载

#### 情况2：用户选择时间晚于数据库最早时间

```
用户选择: 2025-11-01
数据库最早: 2025-09-01

判断: 数据库已有数据
策略: 为保险起见，下载最近7天 (2025-11-27 ~ 2025-12-04)
```

**流程**：
1. 判断数据已有
2. 优化为下载最近7天
3. 调用下载功能
4. 合并到现有数据

## 代码实现

### 核心方法

```python
def _download_minute_bars_from_futu(start, end, progress_callback):
    # 方案1：优先使用 DataManager
    datamanager_engine = self._main_engine.engines.get("DataManager")
    if datamanager_engine and hasattr(datamanager_engine, 'download_history_data'):
        # 调用 DataManager 下载
        datamanager_engine.download_history_data(
            symbol=self._vt_symbol,
            exchange=self._exchange,
            interval=Interval.MINUTE,
            start=start,
            end=end
        )
        
        # 从数据库重新加载
        bars = database.load_bar_data(...)
        return bars
    
    # 方案2：使用 FUTU Gateway 直接下载
    futu_gateway = main_engine.gateways.get("FUTU")
    if futu_gateway:
        req = HistoryRequest(...)
        bars = futu_gateway.query_history(req)
        database.save_bar_data(bars)
        return bars
    
    return None
```

### 优势

- ✅ **复用现有代码**：DataManager 和 FUTU Gateway 的下载逻辑已经稳定
- ✅ **两级后备**：优先 DataManager，失败时用 FUTU Gateway
- ✅ **自动保存**：DataManager 会自动保存到数据库
- ✅ **进度反馈**：通过 progress_callback 实时更新

## 进度更新示例

```
[5%] 正在分析数据库状态...
[8%] 数据不完整，准备从 FUTU API 下载...
[10%] 正在从 FUTU API 下载数据 (2024-01-01 ~ 2025-12-04)...
[12%] 使用 DataManager 下载数据...
      ↓ DataManager.download_history_data()
[20%] DataManager 下载完成，正在从数据库加载...
[22%] FUTU 下载完成: 100000 条
[24%] 正在保存到数据库...
[25%] 数据已保存: 100000 条
      ↓ 继续并行加载大周期...
[35%] 正在并行加载大周期数据 (5m/1H/4H)...
[50%] 5分钟 数据加载完成: 20000 条
[65%] 4小时 数据加载完成: 500 条
[80%] 1小时 数据加载完成: 4000 条
[88%] 正在创建图表项...
[100%] 加载完成！
```

## 使用场景

### 场景1：数据库无数据（新用户）

```
操作: 切换到多周期模式
结果: 自动下载最近7天数据
时间: 约 5-10 秒
```

### 场景2：数据库有旧数据

```
操作: 切换到多周期模式
结果: 保险下载最近7天数据（更新）
时间: 约 3-5 秒
```

### 场景3：需要更早的历史数据

```
操作: 选择起始时间为 3 个月前
结果: 全量下载 3 个月数据
时间: 约 30-60 秒（取决于数据量）
```

## 性能分析

### 数据量估算

- **7天数据**：7 × 24 × 60 = 10,080 条
- **1个月数据**：30 × 24 × 60 = 43,200 条
- **3个月数据**：90 × 24 × 60 = 129,600 条

### 下载时间估算

- **FUTU API 下载速度**：约 1000-2000 条/秒
- **7天**：约 5-10 秒
- **1个月**：约 20-40 秒
- **3个月**：约 60-120 秒

### 优化建议

1. **批量下载**：FUTU API 支持分批下载，可以并行
2. **增量更新**：只下载缺失的部分，不重复下载
3. **后台下载**：在后台线程下载，不阻塞 UI
4. **缓存机制**：缓存已下载的数据，避免重复下载

## 总结

✅ **已完成**：
- 正确的下载策略判断
- 复用 DataManager 和 FUTU Gateway 的下载功能
- 自动保存到数据库
- 详细的进度反馈

🎉 **所有功能已实现，可以正常使用！**

