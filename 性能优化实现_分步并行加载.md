# 性能优化实现：分步并行加载

## 优化策略

### 为什么要分步？

**关键约束**：1分钟数据是基础，其他大周期数据可能依赖于它。

- ✅ **1分钟数据**：必须先完成（如果需要聚合，它是数据源）
- ✅ **大周期数据**：可以并行加载（5m/1H/4H 互不依赖）

### 优化方案：两步加载

```
步骤1: 加载1分钟数据（串行，必需）
    ↓
步骤2: 并行加载 5m/1H/4H 数据（并行，可选）
```

## 实现代码

```python
def _load_data_and_build_items(self, progress_callback=None) -> None:
    from concurrent.futures import ThreadPoolExecutor, as_completed
    
    database = get_database()
    
    # ============================================================
    # 第一步：先加载1分钟数据（必需）
    # ============================================================
    if progress_callback:
        progress_callback("正在加载1分钟K线数据...", 10)
    
    one_minute_bars = database.load_bar_data(
        symbol=self._vt_symbol,
        exchange=self._exchange,
        interval=Interval.MINUTE,
        start=self._start,
        end=self._end
    )
    
    if not one_minute_bars:
        return  # 1分钟数据是必需的
    
    if progress_callback:
        progress_callback(f"1分钟数据加载完成: {len(one_minute_bars)} 条", 30)
    
    # ============================================================
    # 第二步：并行加载大周期数据（5m/1H/4H）
    # ============================================================
    if progress_callback:
        progress_callback("正在并行加载大周期数据 (5m/1H/4H)...", 35)
    
    def load_interval_data(interval: Interval, interval_name: str):
        bars = database.load_bar_data(
            symbol=self._vt_symbol,
            exchange=self._exchange,
            interval=interval,
            start=self._start,
            end=self._end
        )
        return interval_name, bars, None
    
    # 并行加载3个大周期
    tasks = [
        (Interval.MINUTE_5, 'five_minute', '5分钟'),
        (Interval.HOUR, 'one_hour', '1小时'),
        (Interval.HOUR_4, 'four_hour', '4小时'),
    ]
    
    results = {}
    with ThreadPoolExecutor(max_workers=3) as executor:
        futures = {
            executor.submit(load_interval_data, interval, name): display_name
            for interval, name, display_name in tasks
        }
        
        # 按完成顺序获取结果
        completed_count = 0
        for future in as_completed(futures):
            display_name = futures[future]
            interval_name, bars, error = future.result()
            
            if not error:
                results[interval_name] = bars
            
            completed_count += 1
            progress = 35 + (completed_count * 15)  # 50%, 65%, 80%
            
            if progress_callback:
                bar_count = len(bars) if bars else 0
                progress_callback(f"{display_name} 数据加载完成: {bar_count} 条", progress)
    
    five_minute_bars = results.get('five_minute')
    one_hour_bars = results.get('one_hour')
    four_hour_bars = results.get('four_hour')
```

## 时间分析

### 优化前（全串行）

```
1m: 500ms ━━━━━━━━━━┓
5m: 500ms          ┃ 顺序执行
1H: 500ms          ┃
4H: 500ms ━━━━━━━━━━┛
总耗时: 2000ms
```

### 优化后（分步并行）

```
步骤1: 1m 数据（串行，必需）
1m: 500ms ━━━━━━━━━━┓
                   ┃
步骤2: 大周期数据（并行）
5m: 500ms ━━━━━━━━━━┓
1H: 500ms ━━━━━━━━━━┫ 并行执行
4H: 500ms ━━━━━━━━━━┛

总耗时: 500ms + 500ms = 1000ms
```

### 性能对比

- **优化前**：2000ms
- **优化后**：1000ms
- **提升**：**2倍速度** 🚀

## 进度更新示例

```
[10%] 正在加载1分钟K线数据...
[30%] 1分钟数据加载完成: 41280 条      ← 1分钟先完成（必需）
[35%] 正在并行加载大周期数据 (5m/1H/4H)...
[50%] 5分钟 数据加载完成: 8256 条      ← 并行完成（顺序不固定）
[65%] 4小时 数据加载完成: 345 条
[80%] 1小时 数据加载完成: 1377 条
[88%] 正在创建图表项...
[100%] 加载完成！
```

## 优势

### 1. 数据完整性保证

- ✅ 1分钟数据必定先完成
- ✅ 如果需要聚合，可以从完整的1分钟数据聚合
- ✅ 大周期数据加载失败不影响1分钟数据

### 2. 性能提升

- ✅ 大周期数据并行加载，速度提升 2倍
- ✅ 对于大数据量，优势更明显

### 3. 用户体验

- ✅ 1分钟数据快速完成（30% 进度）
- ✅ 大周期数据快速并行完成（30% → 80%）
- ✅ 进度更新流畅

## 适用场景

这个优化特别适合：

1. **大数据量场景**：3个月以上的历史数据
2. **慢速数据库**：网络数据库或云数据库
3. **多用户并发**：多个用户同时加载数据

## 总结

**优化前**：所有周期串行加载 → 2000ms
**优化后**：1分钟先加载，大周期并行 → 1000ms
**提升**：**2倍速度**，同时保证数据完整性！✅

