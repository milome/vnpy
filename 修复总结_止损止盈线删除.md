# 修复总结：止损止盈线删除逻辑

## 问题

模拟止盈后，平仓成功，入场线被删除，但关联的止损止盈线仍然显示。

## 根本原因

**恢复的入场线（`entry_recovered_xxx`）的关联关系只在数据库中，没有加载到内存的 `_entry_line_relations`**

### 为什么会这样？

```
持仓恢复流程:
  1. 从数据库加载入场线（entry_recovered_xxx）✅
  2. 从数据库加载止损线（stop_xxx）✅
  3. 从数据库加载止盈线（profit_xxx）✅
  4. 从数据库加载关联关系 ❌ (没有加载到内存)
  ↓
结果:
  - 数据库中有关联关系
  - 内存 _entry_line_relations 中没有关联关系
  ↓
删除入场线时:
  - 代码检查 _entry_line_relations[entry_recovered_xxx]
  - 找不到 → 不删除止损止盈线 ❌
```

## 修复方案

### 双路径查找关联关系

```python
# 删除入场线时
# 1. 优先从内存查找
if line_id in self._entry_line_relations:
    relations = self._entry_line_relations[line_id]
    # 删除止损止盈线...

# 2. 如果内存中没有，从数据库查找（针对恢复的入场线）✅
if not relations_found and self._price_line_database:
    db_relations = self._price_line_database.get_related_lines(line_id)
    if db_relations:
        # 删除止损止盈线...
```

## 修改内容

### 文件：vnpy/chart/widget_position.py

**修改位置**：第358-413行的删除逻辑

**修改前**：
```python
# 删除关联的止损线和止盈线
if hasattr(self, '_entry_line_relations') and line_id in self._entry_line_relations:
    relations = self._entry_line_relations[line_id]
    # 删除止损止盈线...
else:
    # ❌ 没有 else 分支，恢复的入场线找不到关联关系
```

**修改后**：
```python
# 方法1：从内存的 _entry_line_relations 查找
relations_found = False
if hasattr(self, '_entry_line_relations') and line_id in self._entry_line_relations:
    relations = self._entry_line_relations[line_id]
    relations_found = True
    # 删除止损止盈线...

# 方法2：如果没找到，从数据库查找（针对恢复的入场线）✅
if not relations_found and self._price_line_database:
    self._main_engine.write_log(
        f"[持仓同步] 入场线 {line_id} 在内存中没有关联关系，尝试从数据库查找"
    )
    
    db_relations = self._price_line_database.get_related_lines(line_id)
    if db_relations:
        self._main_engine.write_log(
            f"[持仓同步] 从数据库找到关联关系: {db_relations}"
        )
        
        # 删除止损线
        stop_loss_line_id = db_relations.get("stop_loss")
        if stop_loss_line_id:
            # ... 删除逻辑 ...
        
        # 删除止盈线
        take_profit_line_id = db_relations.get("take_profit")
        if take_profit_line_id:
            # ... 删除逻辑 ...
```

## 完整的删除逻辑

```python
for line_id in lines_to_delete:
    line = self._price_line_manager.get_line(line_id)
    if line:
        # ============================================================
        # 第1步：查找关联关系（双路径）
        # ============================================================
        # 路径1：从内存查找
        if line_id in self._entry_line_relations:
            relations = self._entry_line_relations[line_id]
            # 删除止损止盈线...
        
        # 路径2：从数据库查找（针对恢复的入场线）
        elif self._price_line_database:
            db_relations = self._price_line_database.get_related_lines(line_id)
            if db_relations:
                # 删除止损止盈线...
        
        # ============================================================
        # 第2步：删除入场线本身
        # ============================================================
        # 从 plot 移除
        self._first_plot.removeItem(line)
        # 从管理器删除
        self._price_line_manager.delete_line(line_id)
```

## 数据库的 `get_related_lines()` 方法

```python
# vnpy/chart/price_line_database.py
def get_related_lines(self, entry_line_id: str) -> Dict[str, str]:
    """
    获取入场线关联的止损/止盈线ID
    
    Args:
        entry_line_id: 入场线ID
        
    Returns:
        字典，key 为 "stop_loss" 或 "take_profit"，value 为关联线ID
        例如: {"stop_loss": "stop_xxx", "take_profit": "profit_xxx"}
    """
    relations = self.load_relations(entry_line_id=entry_line_id)
    
    result = {}
    for rel in relations:
        result[rel["relation_type"]] = rel["related_line_id"]
    
    return result
```

## 测试验证

### 测试场景

```
操作:
  1. 模拟成交（创建入场线 + 止损线 + 止盈线）
  2. 重启应用（入场线变成 entry_recovered_xxx）
  3. 模拟止盈（平仓）

预期日志:
  [持仓同步] 标记删除入场线: entry_recovered_c377ded5
  [持仓同步] 入场线在内存中没有关联关系，尝试从数据库查找
  [持仓同步] 从数据库找到关联关系: {'stop_loss': 'stop_3bbaefb94cba', 'take_profit': 'profit_61194a489042'}
  [持仓同步] 已删除关联止损线（从数据库找到）: stop_3bbaefb94cba
  [持仓同步] 已删除关联止盈线（从数据库找到）: profit_61194a489042
  [持仓同步] 已删除入场线: entry_recovered_c377ded5

预期结果:
  - ✅ 入场线被删除
  - ✅ 止损线被删除
  - ✅ 止盈线被删除
  - ✅ 图表上全部清空
```

## 为什么需要两条路径？

### 路径1：内存查找（正常情况）

**适用于**：
- 用户手动创建的入场线
- 关联关系在内存中（`_entry_line_relations`）
- 快速查找

### 路径2：数据库查找（恢复情况）

**适用于**：
- 从数据库恢复的入场线（`entry_recovered_xxx`）
- 关联关系只在数据库中
- 需要额外查询

### 为什么恢复时关联关系没有加载到内存？

可能原因：
1. 恢复逻辑没有调用 `load_all_line_relations()`
2. 或者调用了但时机不对（在创建恢复的入场线之后）

**解决方案**：
- 方案A：恢复时同时加载关联关系到内存
- 方案B：删除时从数据库补充查找（当前方案）✅

方案B更健壮，因为即使恢复逻辑有问题，删除时也能正确处理。

## 总结

**核心修复**：
- ✅ 支持从数据库查找关联关系
- ✅ 针对恢复的入场线的特殊处理
- ✅ 确保止损止盈线被正确删除

**关键点**：
- 双路径查找：内存 + 数据库
- 健壮性：即使恢复逻辑有问题也能正确删除
- 完整性：删除入场线的同时删除所有关联线

修复完成！🎉

