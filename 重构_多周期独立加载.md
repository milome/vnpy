# 重构：多周期独立加载数据

## 核心原则

**多周期模式下的1分钟数据必须严格按照用户指定的时间范围加载**

1. 从单周期切换到多周期不自动同步数据
2. 多周期模式下的数据加载完全独立
3. 1分钟数据正确了，大周期聚合才会正确

## 数据流对比

### ❌ 旧方案：自动同步

```
单周期加载数据:
  load_history_data() 
  → 从数据库加载（可能有优化逻辑）
  → history_data = [...]
  ↓
切换到多周期:
  _switch_to_multi_timeframe_mode()
  → sync_data_to_multi_timeframe()  # 自动同步
  → 使用 history_data 的时间范围
  ↓
问题: 
  - history_data 可能不完整
  - 时间范围可能被优化逻辑修改
  - 1分钟数据不准确 → 大周期聚合错误
```

### ✅ 新方案：独立加载

```
切换到多周期:
  _switch_to_multi_timeframe_mode()
  → 提示用户点击"加载"按钮
  ↓
用户点击"加载":
  refresh_chart()
  → 检测到 display_mode == "多周期叠加"
  → _load_multi_timeframe_data()
  ↓
多周期独立加载:
  1. 获取用户选择的时间范围（从UI控件）
  2. 调用 MultiTimeframeWidget.switch_symbol(user_start, end)
  3. MultiTimeframeWidget 内部：
     a. 从数据库加载1分钟数据（严格按 user_start ~ end）
     b. 从1分钟聚合大周期（使用港期时间划分）
  ↓
结果:
  ✅ 1分钟数据严格符合用户指定范围
  ✅ 大周期聚合正确
  ✅ 完全独立于单周期数据
```

## 修改内容

### 1. `refresh_chart()` 方法

```python
def refresh_chart(self):
    """刷新图表（"加载"按钮回调）"""
    if self.display_mode == "多周期叠加":
        # 多周期模式：独立加载
        self._load_multi_timeframe_data()
    else:
        # 单周期模式：使用原有逻辑
        self.load_history_data()
```

### 2. 新增 `_load_multi_timeframe_data()` 方法

```python
def _load_multi_timeframe_data(self):
    """
    多周期模式下加载数据
    
    关键：
    1. 获取用户选择的时间范围（从UI控件）
    2. 确保1分钟数据严格按照用户指定的时间范围加载
    3. 从1分钟数据聚合大周期
    """
    # 1. 获取用户选择的时间
    user_start = self.start_date_edit.dateTime().toPython()
    end = datetime.now()
    
    # 2. 提取 symbol 和 exchange
    symbol, exchange = extract_vt_symbol(self.current_vt_symbol)
    
    # 3. 调用 MultiTimeframeWidget.switch_symbol
    self.multi_timeframe_widget.switch_symbol(
        symbol=symbol,
        exchange=exchange,
        start_datetime=user_start,  # ✅ 用户选择的起始时间
        end_datetime=end,           # ✅ 当前时间
        progress_callback=...
    )
```

### 3. 修改 `sync_data_to_multi_timeframe()` 方法

```python
def sync_data_to_multi_timeframe(self):
    """
    准备多周期模式的数据加载
    
    注意：不再自动同步单周期数据
    改为提示用户重新加载数据
    """
    self.main_engine.write_log(
        "[多周期] 已切换到多周期模式，请选择起始时间后点击'加载'按钮"
    )
```

### 4. 修改 `process_history_data()` 方法

```python
def process_history_data(self, history):
    """处理历史数据（仅用于单周期模式）"""
    # ... 现有逻辑 ...
    
    # 注意：不再自动同步到多周期模式
    # 多周期模式下的数据加载由 _load_multi_timeframe_data() 独立处理
```

## 关键优势

### ✅ 数据准确性

- 1分钟数据严格按用户指定的时间范围加载
- 不受单周期数据或优化逻辑的影响
- 从1分钟数据聚合大周期，保证一致性

### ✅ 逻辑清晰

- 单周期和多周期完全独立
- 职责分离明确
- 易于维护和调试

### ✅ 用户体验

- 切换模式后，用户明确知道需要重新加载数据
- 可以为多周期选择不同的时间范围
- 加载过程清晰透明

## 使用流程

### 单周期模式

```
1. 选择合约
2. 选择周期（1分钟/5分钟/1小时/4小时）
3. 选择起始时间
4. 点击"加载"按钮
5. 显示单周期图表
```

### 多周期模式

```
1. 选择合约
2. 切换模式到"多周期叠加"
3. 选择起始时间
4. 点击"加载"按钮
5. 显示多周期图表（1m/5m/1H/4H 同时显示）
```

### 模式切换

```
从单周期切换到多周期:
  → 提示用户重新加载数据
  → 用户选择时间后点击"加载"
  → 独立加载多周期数据

从多周期切换到单周期:
  → 用户选择周期和时间
  → 点击"加载"
  → 独立加载单周期数据
```

## MultiTimeframeWidget 的职责

```python
def switch_symbol(self, symbol, exchange, start_datetime, end_datetime, ...):
    """
    加载并显示多周期数据
    
    关键步骤：
    1. 从数据库加载1分钟数据（严格按 start_datetime ~ end_datetime）
    2. 使用 get_period_start() 对5分钟数据分组并聚合
    3. 使用 aggregate_to_1hour_from_minutes() 聚合1小时数据
    4. 使用 get_hkfe_4hour_period() 对4小时数据分组并聚合
    5. 创建并显示图表项
    """
    # 1. 加载1分钟数据
    one_minute_bars = database.load_bar_data(
        symbol, exchange, Interval.MINUTE,
        start_datetime,  # ✅ 严格使用传入的时间
        end_datetime
    )
    
    # 2. 聚合大周期（使用港期时间划分）
    five_minute_bars = aggregate_5min_bars(one_minute_bars)
    one_hour_bars = aggregate_to_1hour_from_minutes(one_minute_bars)
    four_hour_bars = aggregate_4hour_bars(one_minute_bars)
    
    # 3. 创建图表项并显示
    ...
```

## 测试验证

### 场景1：多周期选择2个月前

```
操作:
  1. 切换到"多周期叠加"
  2. 选择起始时间：2025-10-01
  3. 点击"加载"

预期:
  - ✅ 从数据库加载 10-01 ~ now 的1分钟数据
  - ✅ 所有大周期都从这些1分钟数据聚合
  - ✅ 时间轴显示 10-01 ~ 12-04
  - ✅ 数据完整，无错乱
```

### 场景2：单周期和多周期使用不同时间范围

```
操作:
  1. 单周期模式，选择 2025-11-01，加载数据
  2. 切换到"多周期叠加"
  3. 选择 2025-10-01（不同的时间），加载数据

预期:
  - ✅ 多周期独立加载 10-01 ~ now 的数据
  - ✅ 不受单周期数据（11-01 ~ now）的影响
  - ✅ 数据正确
```

### 场景3：模式切换后必须重新加载

```
操作:
  1. 单周期模式，加载了数据
  2. 切换到"多周期叠加"
  3. 不点击"加载"按钮

预期:
  - ✅ 多周期图表为空（或显示提示）
  - ✅ 日志提示用户点击"加载"按钮
```

## 总结

**核心改变**：
- ❌ 不再自动同步单周期数据到多周期
- ✅ 多周期模式下完全独立加载数据
- ✅ 1分钟数据严格按用户指定的时间范围加载
- ✅ 从1分钟数据聚合大周期（使用港期时间划分）

**关键保证**：
- 1分钟数据正确 → 大周期聚合正确 ✅

修改完成！🎉

