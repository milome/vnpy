# 动态画线（跑马灯效果）快速入门

## 快速演示

### Windows

双击运行：
```
run_animation_demo.bat
```

或者在命令行中：
```bash
python examples\drawing\demo_drawing_animation.py
```

### Linux/Mac

```bash
chmod +x run_animation_demo.sh
./run_animation_demo.sh
```

或者：
```bash
python examples/drawing/demo_drawing_animation.py
```

## 5分钟上手

### 第一步：导入类

```python
from vnpy.chart import ChartWidget
from vnpy.chart.drawing_animation import (
    DrawingAnimationManager,
    AnimationLineDirection
)
```

### 第二步：创建管理器

```python
# 假设你已经有了 chart_widget
animation_manager = DrawingAnimationManager(
    widget=chart_widget,
    plot=chart_widget._first_plot
)
```

### 第三步：添加动画线

```python
# 黄色正向跑马灯（价格26000）
line_id = animation_manager.create_horizontal_line(
    price=26000.0,
    color=(255, 255, 0),  # 黄色
    width=3,
    animation_direction=AnimationLineDirection.FORWARD,
    dash_pattern=[15, 5]  # 15像素实线，5像素空白
)
```

### 第四步：管理线条

```python
# 删除指定线条
animation_manager.remove_line(line_id)

# 清除所有线条
animation_manager.clear_all()

# 检查状态
print(f"线条数量: {animation_manager.get_line_count()}")
print(f"动画运行中: {animation_manager.is_running()}")
```

## 常用效果

### 1. 闪烁警戒线（价格提醒）

```python
alert_line = animation_manager.create_horizontal_line(
    price=26500.0,
    color=(255, 0, 0),  # 红色
    width=3,
    animation_direction=AnimationLineDirection.BLINK
)
```

### 2. 正向跑马灯（开盘价）

```python
open_line = animation_manager.create_horizontal_line(
    price=opening_price,
    color=(255, 255, 0),  # 黄色
    width=2,
    animation_direction=AnimationLineDirection.FORWARD,
    dash_pattern=[15, 5]
)
```

### 3. 反向跑马灯（阻力位）

```python
resistance_line = animation_manager.create_horizontal_line(
    price=26800.0,
    color=(255, 165, 0),  # 橙色
    width=3,
    animation_direction=AnimationLineDirection.BACKWARD,
    dash_pattern=[20, 10]
)
```

### 4. 垂直时间标记

```python
time_marker = animation_manager.create_vertical_line(
    time_index=150,  # K线索引
    color=(0, 255, 255),  # 青色
    width=2,
    animation_direction=AnimationLineDirection.FORWARD,
    dash_pattern=[10, 5]
)
```

### 5. 慢速跑马灯（支撑位）

```python
support_line = animation_manager.create_horizontal_line(
    price=25500.0,
    color=(0, 255, 0),  # 绿色
    width=3,
    animation_direction=AnimationLineDirection.FORWARD,
    dash_pattern=[30, 15]  # 更长的虚线，视觉上更慢
)
```

## 颜色参考

常用颜色RGB值：

| 颜色 | RGB | 示例 |
|------|-----|------|
| 红色 | (255, 0, 0) | 警告、止损 |
| 绿色 | (0, 255, 0) | 止盈、支撑 |
| 黄色 | (255, 255, 0) | 开盘价、重要价位 |
| 青色 | (0, 255, 255) | 提醒、当前价 |
| 橙色 | (255, 165, 0) | 阻力位、警戒 |
| 紫色 | (255, 0, 255) | 特殊标记 |
| 粉色 | (255, 192, 203) | 辅助线 |
| 白色 | (255, 255, 255) | 默认 |

## 性能建议

- ✅ **推荐**：< 10条动画线
- ⚠️ **可用**：10-20条动画线
- ❌ **不推荐**：> 20条动画线

## 常见问题

### Q: 如何让跑马灯更快/更慢？

使用更短/更长的虚线样式：

```python
# 快速：短虚线
fast_line = animation_manager.create_horizontal_line(
    price=26000.0,
    dash_pattern=[5, 3]
)

# 慢速：长虚线
slow_line = animation_manager.create_horizontal_line(
    price=26100.0,
    dash_pattern=[30, 15]
)
```

### Q: 可以改变已创建线条的颜色吗？

目前不支持动态修改。需要删除后重新创建：

```python
# 删除旧线
animation_manager.remove_line(old_line_id)

# 创建新线（新颜色）
new_line_id = animation_manager.create_horizontal_line(
    price=26000.0,
    color=(0, 255, 0)  # 新颜色
)
```

### Q: 如何在多个图表中使用？

为每个图表创建独立的管理器：

```python
# 主图管理器
main_animation = DrawingAnimationManager(
    widget=main_chart,
    plot=main_chart._first_plot
)

# 副图管理器
sub_animation = DrawingAnimationManager(
    widget=sub_chart,
    plot=sub_chart._first_plot
)
```

## 更多资源

- **详细文档**：`docs/drawing_animation_guide.md`
- **功能说明**：`FEATURE_动态画线跑马灯效果.md`
- **示例代码**：`examples/demo_drawing_animation.py`
- **单元测试**：`tests/test_drawing_animation.py`

## 技术支持

遇到问题？
1. 查看详细文档
2. 运行演示程序
3. 查看单元测试
4. 提交Issue

祝使用愉快！🎉

