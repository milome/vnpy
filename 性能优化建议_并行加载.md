# 性能优化建议：并行加载多周期数据

## 当前实现：串行加载

### 当前代码

```python
# ❌ 串行执行（顺序加载）
one_minute_bars = database.load_bar_data(..., interval=Interval.MINUTE, ...)      # 耗时 T1
five_minute_bars = database.load_bar_data(..., interval=Interval.MINUTE_5, ...)   # 耗时 T2
one_hour_bars = database.load_bar_data(..., interval=Interval.HOUR, ...)          # 耗时 T3
four_hour_bars = database.load_bar_data(..., interval=Interval.HOUR_4, ...)       # 耗时 T4

总耗时 = T1 + T2 + T3 + T4
```

### 时间分析

假设每个查询耗时 500ms：
- 1分钟数据：500ms
- 5分钟数据：500ms
- 1小时数据：500ms
- 4小时数据：500ms
- **总耗时**：2000ms = 2秒

## 优化方案：并行加载

### 方案A：多线程并行加载（推荐）

```python
# ✅ 并行执行（同时加载）
from concurrent.futures import ThreadPoolExecutor, as_completed

def load_interval_data(interval: Interval):
    return database.load_bar_data(
        symbol=self._vt_symbol,
        exchange=self._exchange,
        interval=interval,
        start=self._start,
        end=self._end
    )

# 并行加载所有周期数据
with ThreadPoolExecutor(max_workers=4) as executor:
    futures = {
        executor.submit(load_interval_data, Interval.MINUTE): 'one_minute',
        executor.submit(load_interval_data, Interval.MINUTE_5): 'five_minute',
        executor.submit(load_interval_data, Interval.HOUR): 'one_hour',
        executor.submit(load_interval_data, Interval.HOUR_4): 'four_hour',
    }
    
    results = {}
    for future in as_completed(futures):
        interval_name = futures[future]
        results[interval_name] = future.result()
        
        # 更新进度
        completed = len(results)
        progress = 20 + (completed * 15)  # 20%, 35%, 50%, 65%
        if progress_callback:
            progress_callback(f"{interval_name} 数据加载完成", progress)

one_minute_bars = results['one_minute']
five_minute_bars = results['five_minute']
one_hour_bars = results['one_hour']
four_hour_bars = results['four_hour']

总耗时 = max(T1, T2, T3, T4) ≈ 500ms
```

### 性能提升

**串行**：2000ms
**并行**：500ms
**提升**：**4倍速度提升**！

## 实现建议

### 修改 `_load_data_and_build_items()` 方法

```python
def _load_data_and_build_items(self, progress_callback=None) -> None:
    from concurrent.futures import ThreadPoolExecutor, as_completed
    
    database = get_database()
    
    if progress_callback:
        progress_callback("正在并行加载多周期数据...", 10)
    
    # 定义数据加载函数
    def load_data(interval: Interval, interval_name: str):
        bars = database.load_bar_data(
            symbol=self._vt_symbol,
            exchange=self._exchange,
            interval=interval,
            start=self._start,
            end=self._end
        )
        return interval_name, bars
    
    # 并行加载所有周期数据
    tasks = [
        (Interval.MINUTE, 'one_minute'),
        (Interval.MINUTE_5, 'five_minute'),
        (Interval.HOUR, 'one_hour'),
        (Interval.HOUR_4, 'four_hour'),
    ]
    
    results = {}
    with ThreadPoolExecutor(max_workers=4) as executor:
        # 提交所有任务
        futures = {
            executor.submit(load_data, interval, name): name
            for interval, name in tasks
        }
        
        # 按完成顺序获取结果
        completed_count = 0
        for future in as_completed(futures):
            interval_name, bars = future.result()
            results[interval_name] = bars
            
            completed_count += 1
            progress = 10 + (completed_count * 15)  # 25%, 40%, 55%, 70%
            
            if progress_callback:
                bar_count = len(bars) if bars else 0
                progress_callback(f"{interval_name} 数据加载完成: {bar_count} 条", progress)
    
    # 提取结果
    one_minute_bars = results['one_minute']
    five_minute_bars = results['five_minute']
    one_hour_bars = results['one_hour']
    four_hour_bars = results['four_hour']
    
    # ... 后续创建图表项的代码保持不变 ...
```

## 注意事项

### 1. 线程安全

- ✅ 数据库读取操作（`load_bar_data()`）是线程安全的
- ✅ 数据加载可以在后台线程并行执行
- ❌ Qt 对象创建必须在主线程（已经在主线程执行）

### 2. 资源消耗

- **CPU**: 并行加载会使用多个CPU核心
- **内存**: 同时加载4个周期的数据，内存使用略高
- **数据库连接**: 需要确保数据库支持多连接

### 3. 进度更新

并行加载时，进度更新不是线性的（哪个周期先完成就先更新），但用户体验更好：
```
[10%] 正在并行加载多周期数据...
[25%] five_minute 数据加载完成: 8256 条
[40%] one_minute 数据加载完成: 41280 条
[55%] four_hour 数据加载完成: 345 条
[70%] one_hour 数据加载完成: 1377 条
[85%] 正在创建图表项...
[100%] 加载完成！
```

## 性能对比

### 串行加载（当前）

```
时间轴: ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        1m──5m──1H──4H  (顺序执行)
        500  500 500 500
总耗时: 2000ms
```

### 并行加载（优化后）

```
时间轴: ━━━━━━━━━━━━
        1m────┐
        5m────┤
        1H────┼─ (并行执行)
        4H────┘
         500ms
总耗时: 500ms
```

## 实施建议

### 优先级：中

**优点**：
- 速度提升 4倍（对于大数据量）
- 用户体验显著改善
- 代码改动量小

**缺点**：
- 需要确保数据库支持并发
- 增加了一点代码复杂度

### 实施步骤

1. 修改 `_load_data_and_build_items()` 方法
2. 添加并行加载逻辑
3. 更新进度回调机制
4. 测试验证
5. 性能基准测试

## 总结

当前是**串行加载**，可以优化为**并行加载**，速度提升约 **4倍**。

对于大数据量（如 3个月的1分钟数据），效果更明显：
- 串行：可能需要 5-10 秒
- 并行：只需要 1-3 秒

建议在后续版本中实施此优化。

