# 正确的数据下载策略

## 用户需求理解

### 策略1：用户选择时间 < 数据库最早时间

```
用户选择: 2024-01-01
数据库最早: 2025-09-01

判断: 数据库数据不全
策略: 从 FUTU API 全量下载 (2024-01-01 ~ 2025-12-04)
```

**原因**：
- 用户想看更早的数据
- 数据库没有那么早的数据
- 需要从 FUTU API 补齐完整的历史数据

### 策略2：用户选择时间 >= 数据库最早时间

```
用户选择: 2025-11-01
数据库最早: 2025-09-01

判断: 数据库已有数据
策略: 为保险起见，重新下载最近7天 (2025-11-27 ~ 2025-12-04)
```

**原因**：
- 数据库已有数据，但可能不是最新的
- 为保险起见，重新下载最近7天的数据
- 确保数据是最新、最准确的

### 策略3：数据库无数据

```
数据库: 空

判断: 数据库无数据
策略: 从 FUTU API 下载最近7天
```

## 实现逻辑

### 核心方法：`_get_optimized_start_time()`

```python
def _get_optimized_start_time(self, database) -> tuple:
    """
    Returns:
        (optimized_start, need_download_from_futu)
    """
    # 查询数据库最早数据
    db_earliest = query_database_earliest(...)
    
    if user_start < db_earliest:
        # 情况1：数据不全，全量下载
        return user_start, True
    else:
        # 情况2：数据已有，保险下载最近7天
        return datetime.now() - timedelta(days=7), True
```

### 数据加载流程

```python
optimized_start, need_download = self._get_optimized_start_time(database)

if need_download:
    # 从 FUTU API 下载
    one_minute_bars = self._download_minute_bars_from_futu(
        optimized_start,
        self._end,
        progress_callback
    )
else:
    # 从数据库加载
    one_minute_bars = database.load_bar_data(...)
```

## FUTU API 下载实现

### 方案：通过 MainEngine 调用

```python
def _download_minute_bars_from_futu(
    self,
    start: datetime,
    end: datetime,
    progress_callback=None,
    main_engine=None  # 需要传入 MainEngine
) -> list:
    """从 FUTU API 下载1分钟历史数据"""
    
    if not main_engine:
        logger.error("需要 MainEngine 才能访问 FUTU Gateway")
        return None
    
    # 1. 获取 FUTU Gateway
    futu_gateway = None
    for name, gateway in main_engine.gateways.items():
        if name == "FUTU" or "futu" in name.lower():
            futu_gateway = gateway
            break
    
    if not futu_gateway:
        logger.error("未找到 FUTU Gateway")
        return None
    
    # 2. 构造历史数据请求
    req = HistoryRequest(
        symbol=self._vt_symbol,
        exchange=self._exchange,
        interval=Interval.MINUTE,
        start=start,
        end=end
    )
    
    # 3. 调用 query_history
    if progress_callback:
        progress_callback(f"正在从 FUTU API 下载数据...", 15)
    
    bars = futu_gateway.query_history(req)
    
    if progress_callback:
        progress_callback(f"FUTU API 下载完成: {len(bars)} 条", 25)
    
    # 4. 保存到数据库（可选）
    if bars:
        database.save_bar_data(bars)
        logger.info(f"已保存 {len(bars)} 条数据到数据库")
    
    return bars
```

### 集成到 ChartWindow

需要在调用 `switch_symbol()` 时传入 `main_engine`：

```python
# ChartWindow.sync_data_to_multi_timeframe()
self.multi_timeframe_widget.switch_symbol(
    vt_symbol=symbol,
    exchange=exchange,
    start=start_datetime,
    end=end_datetime,
    progress_callback=on_progress,
    main_engine=self.main_engine  # 传入 MainEngine
)
```

## 待实现功能

### 当前状态

- ✅ 下载策略逻辑已实现
- ⏳ FUTU API 下载功能待实现（需要 MainEngine 支持）
- ⏳ 数据保存到数据库功能待实现

### 实施步骤

1. **Phase 1**（当前）：
   - ✅ 实现下载策略判断逻辑
   - ✅ 添加占位方法 `_download_minute_bars_from_futu()`

2. **Phase 2**（后续）：
   - ⏳ 修改 `switch_symbol()` 接受 `main_engine` 参数
   - ⏳ 实现 `_download_minute_bars_from_futu()` 完整逻辑
   - ⏳ 添加数据保存到数据库功能

3. **Phase 3**（优化）：
   - ⏳ 增量下载（只下载缺失的部分）
   - ⏳ 批量下载优化
   - ⏳ 下载进度详细显示

## 暂时的解决方案

**当前建议**：在切换到多周期模式前，先在单周期模式下点击"更新数据"按钮，确保数据库有完整的1分钟数据。

**原因**：
- FUTU API 下载功能需要 MainEngine 支持
- 单周期模式已有完整的下载和补齐逻辑
- 可以复用现有功能

## 总结

**正确的策略**：
1. 用户选择早于数据库 → 数据不全 → 全量下载
2. 用户选择晚于数据库 → 数据已有 → 保险下载最近7天
3. 数据库无数据 → 下载最近7天

**当前实现**：
- ✅ 策略判断逻辑已实现
- ⏳ FUTU API 下载功能待实现（Phase 2）

